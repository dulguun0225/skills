---
name: manual-only
description: Gate canary fixture, not a real skill, proving description-budget.mjs reads disable-model-invocation and leaves this description out of the set total.
disable-model-invocation: true
---

Gate canary: this fixture is not a skill. It exists so `npm run check:descriptions` can prove it still reads `disable-model-invocation: true` as leaving a description out of the set total. It is deliberately never placed under `skills/`.
