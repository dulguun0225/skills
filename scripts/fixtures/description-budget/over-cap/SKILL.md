---
name: over-cap
description: This is a gate canary fixture, not a real skill, used to prove description-budget.mjs still refuses an over-length description. This is a gate canary fixture, not a real skill, used to prove description-budget.mjs still refuses an over-length description. This is a gate canary fixture, not a real skill, used to prove description-budget.mjs still refuses an over-length description. This is a gate ca
---

Gate canary: this fixture is not a skill. It exists so `npm run check:descriptions` can prove it still refuses a description over the 400-char cap. It is deliberately never placed under `skills/`.
