# The `primary-keys` skill

*Extracted from `CLAUDE.md` 2026-08-02, unedited except that cross-section pointers became file links. Where older text says "this file", it means the project record, which was one file then.*


Authored 2026-08-01, straight after `ai-maintainer-principles`. **Third harvested from `BACKLOG.md`, fourth not drawn from deleted corpus.** Two files:

```
skills/primary-keys/  SKILL.md  evidence.md   15 directives + worked case + losers table, no ids
```

Nineteenth skill, listed by `npm run check`. **No row in [the decomposition record](decomposition.md)** — that table = corpus provenance, this have none. Drawn from sibling repo `../net-saas`: its primary-keys record (2026-06-12) **plus five neighbours that record name or is named by** — business numbering, API contract conventions, schema-per-tenant, data protection, ledger — each read, plus a further set opened for one fact each and named in `evidence.md`. **`../raw` searched: it hold the backlog row naming "UUIDv7 research notes" and no notes**, same result as `ai-maintainer-principles`. **Row naming research notes have now twice named a hope, not a location.**

### What it decided

1. **Same two-half shape as `backend-stack` + `guardrails-toolchain`** — portable criterion + dated worked case. Same pass, same premise, same absence of per-claim markers, so shape followed not re-derived.
2. **Subject = what identify a row, and what that identity may never mean.** No skill had it. `money-storage` constrain a row's *contents*; `java-backend-api` own pagination and carry a pager carve-out **conditioned on a ban no skill published**; `java-backend-rules` require committed migrations and lint them for lock hazard and **say nothing about the key column at all**. So agent writing first migration on that stack reach for corpus default — `bigserial` or `gen_random_uuid()` — and nothing notice.
3. **Central claim: key type decided by surfaces the id land on, not by storage cost.** Marked *uncertain* — fourth skill in set whose central claim is, on no measurement. **Criterion is conversion's generalisation and `evidence.md` say where the seam is**: record state a context paragraph and a list of project facts, no criterion; every loser's decisive ground in it turn out to be a surface, which is what make the generalisation faithful rather than invented.
4. **Source's provenance is better than three sibling skills' and still convention.** Its status line record a **three-way adversarial evaluation** — UUIDv7 steelman, bigint steelman, hostile audit of hybrid and TSID — with grounds per loser written down. **It still cite no primary source for anything**, and `tech-decision-research` grade *confirmed* on votes cast **against primary sources**. Stated precisely in both files rather than smoothed either way, cuz "better than the others and still convention" is exactly the reading that decay into one or the other.
5. **Reading a neighbour changed a directive, first time in a harvest here.** Primary-keys record put its own sequence-reset obligation at a "single-digit checklist"; business-numbering record, **same day**, resolve it to **zero** — counter rows replicate as data, sequences do not, so the carve-out stand deliberately unexercised. From the first record alone the rule would have read *keep the checklist short*. It read *count the steps, and zero differ in kind from small*.
6. **Benchmark carried with its numbers, named unreproduced in three places.** 3,420 against 3,480 transactions per second, no harness, hardware, dataset or methodology recorded. **Range would misrepresent what the pass claimed**; do-not-cite entry cover it, and reproducing it is the cheapest thing anyone could do to change this skill's confidence.
7. **The folklore directive is the one most likely to outlive its numbers.** Two corrections carried — anti-UUID performance numbers are UUIDv4 numbers, and *fat key multiplies every index* is InnoDB lore false in PostgreSQL where secondary indexes point at heap tuple identifiers. Directive is *check the folklore against your own engine and record what you checked*, so it survive every figure in it decaying.
8. **Business-numbering machinery deliberately not carried, and promoted to a backlog row instead.** Number classes, counter mechanics and contention arithmetic, typed-parts format model, check-digit selection, exhaustion rules = a skill's worth of material. Carried only what the key decision rest on: two identifiers with disjoint namespaces, and that the sequential one is not a sequence. **`BACKLOG.md` gained a row it did not have** — first row added by a harvest rather than salvaged from corpus.
9. **No rule ids, `###` headings**; **`## Wiring the gates` present** — these directives bind a build.
10. **One clause marked as the conversion's own, inside the directive not only in `evidence.md`.** A time-ordered key retain its row's creation instant **after anonymization**, so a repo whose erasure story lean on the key as pseudonym retain one more fact than intended. Neither record state the interaction: one argue timestamp disclosure acceptable cuz creation timestamp is published anyway, other retain the key as pseudonym — but a published column can be tombstoned and the instant inside the key cannot. Conversion-dated **2026-08-01** and attributed to nobody.

### The sweep

**Two live pointers, both narrowings, neither a replacement:**

- **`java-backend-api`** — *The pager is the one carve-out from a synthetic-id sort ban* shipped conditional ("if this repo bans `ORDER BY` on synthetic id column") and **dormant, cuz no skill in this set published that ban.** It now resolve, and the directive name where. **Condition kept, cuz it stay real** for a repo installing one skill and not the other. **And reading the other side found the sharper half:** the carve-out have **four** constraints and that skill state **two** — never-leading-sort and pager-scoped exemption. That id never appear in the declared `sort` vocabulary, and that **tie order be an explicit non-promise in contract text**, are stated nowhere in it. Draft said "(above)" for the first of those; it is not above, it is absent. **Follow-the-pointer finding, caught during authoring by opening the file instead of trusting the memory of it.**
- **`java-backend-rules`** — **gap 10 added**: no rule there say what a key column is, how it is generated, or what may be assumed about its order, and its migration lint no ask. Same shape as gap 9, which `guardrails-toolchain` added, and the startup-loud pointer `ai-maintainer-principles` added. **Gap stated, not closed there**, cuz the new skill is stack-neutral and that one carry no key rule to narrow.

**Nothing else needed sweeping, and that checked rather than assumed:** grep for absence-asserting sentences across all published skills found none about keys. **`async-handoff` `E-7` collision published from this skill's side only** — `E-7` require message identity be a **deterministic function of committed inputs** so redelivery re-derive it; a row key is deliberately not that, it is assigned once from a clock and a counter. Both readings verified by opening `E-7` before writing about it. Carrying either rule to the other side breaks something: a derived row key give up insert locality, a clock-assigned message identity break replay dedupe.

**`README.md` swept** (row + marker-ceiling sentence, now four convention-only skills not three, with the engine-fact decay caveat beside the tool-fact one). **`BACKLOG.md`**: row deleted from *Researched, unwritten*, five residues promoted to rows of their own in *Evidence owed*, `business-numbering` added as a new row, `postgres-tenancy` row narrowed to say its record was opened for one fact and is otherwise unread, and **a `primary-keys` row added to the incompleteness-check table where the predicate check most likely to bite** — the criterion is an enumeration of surfaces given by example, which is the exact shape that check was written against, and **this time the skill's own gap list name it**, which earlier failures of that check did not.

## The three incompleteness checks, 2026-08-02

**First backlog item cleared from the *Checks never run over the published skills* table.** Both checks are a read of the file asking one question per directive, and both bit. **Neither promoted a marker** — the table's own rule, and there was nothing to promote: every finding was an absent check, not a verified claim.

### What the layer check found

1. **One false coverage claim, which is the finding that mattered.** *A time-ordered key is not an ordering* named an architecture test as its only host, and the named gap beside it said the ban "catch the SQL". **ArchUnit reads bytecode and never reads query text**, so the gap list asserted the opposite of the truth. This is the *false green* shape `enforceable-rules` calls worse than a missing gate, and the repo had **already published the remedy**: `money-storage` `M-35` is an ArchUnit predicate **plus** a lint over committed query text, with the runtime-assembled blind spot stated beside it. `primary-keys` had the neighbour's finding available and did not carry it across. **The check that catches this is exactly the layer check, and nothing else here would have.**
2. **A second language named beside seven more check lines** — *Count the manual steps* (a sequence is also declared by a mapping annotation and by a non-SQL changelog, where the token is not `CREATE SEQUENCE`), *The schema default is the backstop* and *Exactly one application-side producer* (the version-4 generator's application-side twin — `UUID.randomUUID()`, `uuid.uuid4()`, `crypto.randomUUID()` — named for the first time; the migration grep cannot see it, and the client that supplies its own id is compiled by nobody here), *The opaque key and the human-facing number* (the ban covers reports; the lint reads code), *A key is never a capability* (the probe matrix is endpoint-shaped, and a message consumer, a batch job and a pre-signed URL dereference the same id), *Ids cross the wire as strings* (the document declares, the serializer emits), *The key is what survives erasure* (a path concatenated at runtime, landing in a control-plane log this repo cannot sweep).
3. **Three cleared, stated so the blank is not read as coverage**: *One mechanical key rule* and *A pure child* were already written across the foreign-key graph and the API document, and *A library's own tables* governs a value that never leaves the schema.
4. **One left open rather than closed.** The enumerable-key contract lint reads the committed API document; a route declared only in framework annotations binds an id that document never mentions. The route-table assertion that would reach it is **not** carried — writing it would be a new directive at the normal bar, which a check may not do on its own.

### What the predicate check found

**The load trigger was framed on the technology in the skill's name.** "Load before creating a table" — while three directives govern acts nobody creates a table to perform: writing an object-storage key template, designing a human-facing number format, and generating an id in application code. **The last is the sharp one**: the skill bans the version-4 generator in the schema and the cheapest way to reach for it is a line of application code the predicate never fired on. Description widened, `npm run check` re-run.

### What the composite-shape check found

**It produced *Composite shapes a repo assembles out of the key*, and the split inside that table is the finding.** Most entries were **already decided by a published directive and never stated as a shape** — the key and the business number in one payload, the business number as a URL identifier, the key rendered into a document, the key in an outbox row. A reader looking for the shape rather than the directive found silence, which is exactly what the check says silence reads as.

**Four entries were decided by nothing at all:**

1. **The key as a range-partition key.** The insert locality the whole verdict rests on becomes a hot partition one level up. **The same fact is booked as the benefit under *Check the key-cost folklore* and as the cost here**, and neither side is measured anywhere in this set. Marked *permitted with conditions, and the conditions are unmeasured* rather than decided.
2. **A composite child key three levels deep.** A grandchild's foreign key makes the middle table referenced, so it fails the pure-child test and takes a surrogate. **The published test caps the chain at one level and nobody had said so** — derived from the directive, not newly banned.
3. **The key in a reporting store.** The `ORDER BY` query-text lint added by the layer check reads this repo's SQL; a business-intelligence tool's own query layer is outside it, and that is where a sort on an id is most likely to be written.
4. **A key derived from another identifier** — a hash, a UUIDv5, any deterministic function of committed inputs. **The one ban the check produced**, carrying the ground (a derived key is placed by its hash, so it scatters inserts — the behaviour the time-ordered choice exists to avoid — and where the derivation reads a natural key it is derivable from personal data, which the erasure directive bans outright), the organisation fact, the absence of a panel, and the re-open condition. **`async-handoff` `E-7` is explicitly not that case** and the ban says so, because the two identifiers had already been confused once in this skill's own authoring.

**Three entries were handed to skills that already own them, each verified by opening the file:** `caching` `C-6` (the cache key is the loader's full argument tuple; hand-built key strings banned), `money-api` `M-17` (the idempotency key, which a client-supplied create id is routinely confused with — and this skill's *Exactly one application-side producer* names that create case, so the confusion was reachable from inside), and `java-backend-observability` for request identity.

### The sweep

- **`java-backend-observability` gained a pointer, and it is the one worth recording.** Its *Domain types are unloggable by type* directive, written 2026-07-27 by a different pass from a different source, says *log entity ids, never names or account numbers*. That is **the same two-identifier split and the same log rule** as *The opaque key and the human-facing number* and *The key is what survives erasure*, reached without either pass reading the other. **This is the closest thing `primary-keys` has to independent corroboration**, and it corroborates the split only — nothing about the key choice, the benchmark or the engine facts. Both files now say so from their own side, and both say what it does not cover.
- **`java-backend-rules` gap 10 narrowed, not replaced.** It said the `ORDER BY` ban is one "this skill's ban-list test class can host". Half true now: the class hosts the jOOQ-builder half soundly, and that stack carries **no lint over view text, function bodies or migrations** — squawk reads migrations for lock and rewrite hazards, not sort columns. **The stack narrows the exposure rather than closing it**, because plain-SQL `String` constructs are already banned there, so `ORDER BY id` in a string is largely unwritable in application code.
- **`java-backend-api` line 164 checked and left alone.** It says a repo wiring the pager off that skill alone gets "the ArchUnit half and neither contract half" — still true, and it is a claim about that skill's own coverage, not about whether ArchUnit suffices.
- **`README.md` row widened** to match the new predicate.
- **Both `primary-keys` files edited in the same pass**, deliberately: `SKILL.md` per directive and `evidence.md` in a new *The incompleteness checks, run 2026-08-02* section. **The repo's worst recorded shape is fixing one and missing the other**, and this pass had the reverse risk — findings that live naturally in evidence while the always-loaded file keeps the stale check line.

### Still open

- **The enumeration and token-placement checks have not been run on this skill.** Both are now the cheapest remaining work on it: the token-placement check has a mechanical form and this skill's source is a set of records in `../net-saas` that can still be extracted from, and the enumeration check is a read for counts and superlatives — **this pass created candidates for it**, since three sections written today state what a check found.
- **The partition-key entry is the shape table's weakest.** It is marked *permitted with conditions, and the conditions are unmeasured*, which is honest and decides nothing. It closes when anyone measures insert distribution under a range-partitioned time-ordered key — the same class of measurement as the unreproduced benchmark, and it would inform both.
- **Benchmark unreproduced, engine facts unsourced.** Both in `BACKLOG.md`. Engine facts are **unsourced, not unobtainable** — checkable from vendor documentation by anyone — which is a better position than the three sibling skills' grounds and still not a source.
- **No re-open trigger per loser**, so worked case half-fail `backend-stack`'s *Record the losers and their grounds*, **exactly as `backend-stack`'s own worked case does**. Inventing one rejected, same call, same reason.
- **Cost never measured at the volume where the recorded trigger fire.** The trigger is measured against a **committed bigint baseline**, so the measurement is defined and simply untaken.
- **No adversarial review run on this skill.** Both preceding harvests had a fresh-context subagent audit with a planted canary, and both audits found confirmed defects in files the authoring pass had called clean. This one has had authoring plus the checks named above.

