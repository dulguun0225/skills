# The `ai-maintainer-principles` skill

*Extracted from `CLAUDE.md` 2026-08-02, unedited except that cross-section pointers became file links. Where older text says "this file", it means the project record, which was one file then.*


Authored 2026-08-01, straight after `guardrails-toolchain`. **Third skill not drawn from deleted corpus, second harvested from `BACKLOG.md`.** Two files:

```
skills/ai-maintainer-principles/  SKILL.md  evidence.md   12 directives + worked case + counterfactual, no ids
```

Eighteenth skill, listed by `npm run check`. **No row in [the decomposition record](decomposition.md)** — that table = corpus provenance, this have none. Drawn from sibling repo `../net-saas`: **the architecture decision records** of same 2026-06-11..14 pass that ground `backend-stack` + `guardrails-toolchain`, plus one non-authoritative counterfactual variant and one note written for management.

### What the row promised and what was actually there

**Row named "AI-maintenance research notes". No such notes exist** — no in this repo, no in `../raw` (undeleted corpus working copy, which hold only the row itself), no in external repo. **First harvest where named source turn out to be name of hope not location.** What exist = decision records — boundary criteria, service decomposition, stack chassis, verification strategy, local verification, predecessor scoping, code layout, plus a non-authoritative counterfactual variant and a note written for management — and topic had to be **assembled across them** rather than read out of one. Opposite shape from `guardrails-toolchain`, whose source was one consolidated map and whose expensive half was establishing what published skills already owned.

**Cost estimate held anyway — day, not research project — for different reason**, recorded in `BACKLOG.md`.

### What it decided

1. **Subject = system shape, cuz that what no skill had.** `enforceable-rules` state what shape a *rule* take, `backend-stack` which stack, `guardrails-toolchain` which gates, `tech-decision-research` how to reach verdict. Nothing said how system itself get cut when maintainer is agent. Directive set split four ways: what maintainer cannot see (startup-loud allowance, whole-program reasoning, out-of-repo state), how system cut (session-sized module, count the wills, one idiom), review model (steering human is not reviewer, fresh-context substitute, predecessor not oracle, zero retries), and what maintainer do alone (bus factor by failure shape, hand-built subtle piece).
2. **Premise gain second half no other skill carry.** Every skill state *code written by LLM agents, no human read it line by line*. This one add the records' own two sentences: **runtime behaviour absent from program text invisible in absolute sense** (agent simulate framework from corpus rather than observe it), and **context locality = characteristic failure mode** (one file at a time, so requirement needing whole-program reasoning degrade every session). Second half is what most directives actually hang on — absent *reader* no enough to derive them.
3. **Startup-loud allowance = the find.** `enforceable-rules` *No silent runtime behaviour* ban ambient dispatch; `java-backend-rules` carry per-mechanism list. **Neither state why boot-failing machinery survive**, and repo carrying that list without discriminator either ban its own dependency injection or relax list until nothing banned. **Gap fixed in `java-backend-rules` too**, same shape as `guardrails-toolchain` gap 9 — pointer added at ban-list head.
4. **Eight-principle overlap was the real constraint, and row no predict it.** Row predicted `backend-stack` overlap (correct — two of four topics already published there, checked first). Third overlap = `enforceable-rules` eight principles. **Resolution: this skill state system shape, that one state rule shape; principles cited by name, none restated.** Sharpest case = *A predecessor system is an inventory, not an oracle*, which **pass *gates need an outside oracle* by letter and fail it in substance** — predecessor output *is* outside authoring model. Skill state resolution inline: oracle's authority come from being right, not from being external. **First place published skill narrow published principle rather than instantiate it.**
5. **Counterfactual carried as method, not verdict.** Pass wrote non-authoritative variant re-deriving whole decomposition under one changed axiom (many teams, one deployable each), keeping everything else. Shipped cuz it show *which* decisions premise carry — and with two honesty notes: same pass arguing with itself, and its conclusion (decision robust to premise it no optimised for) = comfortable finding for its author.
6. **Worked case list decisions, never modules.** That repo's tiers, module names, tenancy model, ledger design all project-shaped and left out. Table map decision to directive it instantiate.
7. **No rule ids, `###` headings**; **`## Wiring the gates` present** — four directives have real build gates (boundary tests, formatter, retry setting, quarantine expiry), rest are written artifacts.
8. **Popularity numbers deliberately no carried.** Record compare star counts across three named libraries, against frontend frameworks as a class, to show metric mislead. Reasoning carried, numbers no — they were approximate when written and decay. Same call `guardrails-toolchain` made on version pins.

### The sweep

**Prior-art sentences narrowed, not replaced**, per `llm-default-traps` lesson:

- **`backend-stack/evidence.md`** — its "three records carry it" note now say persistence ADR's *other half* (governing principle + context-locality premise) restated here, cuz that half no stack-choice criterion. Weight unchanged: still prior art, still no per-claim marker.
- **`java-backend-rules/evidence.md`** — 2026-06-11..14 platform pass now restated in **two** skills, neither of them that one; split stated as what write-once force — ground travel once, checks stay with stack.
- **`java-backend-observability` deliberately left alone.** Its prior art = observability decision record, no read for this skill and no published. Wholesale replacement would have been false. Same call `guardrails-toolchain` made about that same file.

**Two skills depended on a rule this one now owns.** `java-backend-api` (SKILL + evidence) and `money-java/api.md` each carry zero-test-retry as "this rule set's own governance choice, not an external precondition". Now name this skill — **and all three keep original do-not-cite warning**, cuz publishing rule here no make it a Schemathesis requirement, which is what those entries exist to deny. **Narrowing, not replacement, in its cleanest form yet: the old sentence stay true word for word and gain a pointer.**

**`README.md` swept** (row + marker-ceiling sentence now name three convention-only skills, no two). **`BACKLOG.md`**: row deleted from *Researched, unwritten*, residues promoted to four rows in *Evidence owed*, and **predicate-check row added where it most likely to bite** — three directives quantify over open set named only by example (*every operational surface*, *every load-bearing dependency*, *anything subtle*), which is exact shape that check written against, and skill's own gap list no name it.

### Still open

- **"Fits one session" unmeasured, in any unit.** No record state threshold — no file count, no line count, no token budget. Weakest directive here on enforcement, and one most easily applied after fact to justify split somebody already wanted.
- **Central claim *uncertain* on no measurement at all.** One repo built this way, none built other way. **Recorded failure worth as much as success.**
- **Counterfactual never run hostile.**
- **Primary sources owed, same search, same result as two sibling skills.** Closes for all three or not at all.
- **Adversarial review run — see *The hostile audit* below.** What still open after it: everything above, plus reading window it exposed — this skill assembled from nine documents in source repo that hold many more, and two of its confirmed defects came from records adjacent to ones it read.

### The hostile audit, 2026-08-01

**Second fresh-context subagent audit in this repo, second carrying planted canary.** Canary = marker-vocabulary ownership inverted in *Where the rest of this lives* — `tech-decision-research` credited with enforcement markers, `enforceable-rules` with confidence markers, **both backwards**, and file's own neighbouring bullet contradicting it eleven lines up. Every mechanical sweep pass it: both skills exist, both vocabularies exist, ids resolve, frontmatter parse. **Audit caught it, ranked it first.** Vocabulary split = that pair's own decision ([the method-skills history](method-skills.md)), so it also pair most likely to break it — recorded there, now demonstrated from outside.

**Structure held**: 12 directives each carry check + marker + date, marker table match check lines exactly, no `P-n`, no `B-n`, no `DECISIONS.md`, no ADR number, no external repo path, zero links so none can leave skill dir, eighteen skills listed matching `ls skills/`. Eleven confirmed defects fixed across six files, plus nine plausible ones, all addressed. **Five findings generalise:**

- **Two source documents fused into one, and fusion made claim false of document whose axiom it stated.** Skill carried "the counterfactual" as single exercise. It two: non-authoritative team-per-deployable variant (**undated**, split transactional core under force — its own heading say "the one consequence that is genuinely forced") and scenario analysis in management note (2026-06-13, one deployable kept, core no move). Skill asserted core stay inseparable **and dated claim from the note** — true of second, false of first, date belonged to neither. **Where two source documents run same *kind* of exercise, merging them is the failure, cuz what survive is exactly what differ between them.** Both now named separately with disagreement stated.
- **Worked case can promote a rejected alternative's acknowledged upside into winner's feature list.** Skill listed boot-time schema validation among machinery repo kept as startup-loud. Record mention it **only inside rejected JPA alternative**, as that profile's one acknowledged advantage, answered by committed-codegen diff gate. Nothing structural see this — item real, source real, only its side of argument wrong.
- **Discriminator shipped as binary where source had third branch, and third branch was load-bearing one.** Record keep five framework pieces; **two neither boot-failing nor banned** — security filter chain, event registry named "the one accepted runtime-trust piece" — each accepted at a price, standing crash-recovery test and per-endpoint authorization probes. Skill argued its own value as *the allowance half stop the ban becoming useless*, then dropped the allowance that need pricing. **Directive arguing for nuance must carry source's own hardest case, or it publish the binary it warn against.**
- **De-naming, again, and this time in skill that name none.** Token extraction found **one product name per file**. Worked case hedged jOOQ, Hibernate, Spring Data JPA, palantir-java-format, Spotless, Picnic error-prone-support, PIT, `DSLContext` into categories — and `enforceable-rules` *Distrust what the agent picks* say plainly **do not swap named loser for category**. Worse, do-not-cite entry warned against citing "the framework and library names that appear in the worked-case table", **where none appeared** — unobeyable entry, shape second Java-backend review ranked second. Cause = habit carried from neutral skills; this skill have no stack sibling, so nowhere else for names to live. Now named.
- **Rejection grounds carried from wrong version of the loser.** Skill said dominant persistence library rejected on flush, proxy, cascade semantics. Record evaluated it **in its best form — no persistence context, no dirty checking, no lazy loading, no cascades** — and rejected *that* on four grounds, of which corpus gravity load-bearing and flush appear only as lapse risk. **Skill published rejection of the default where record ran rejection of the steelman**, inverting what `tech-decision-research` *Steelman the loser* exist to produce.

Rest, no generalise: "four have real build gates" enumerated four where five directives carry off-the-shelf marker, included quarantine expiry its own check line mark **bespoke**, omitted two — now named not counted, wiring section's step-range split replaced same way; **"four files"** invented in always-loaded file where `evidence.md` one hop away carry source's actual phrase (session, dirty state, flush point); pointer said `guardrails-toolchain` carry the formatter row — it carry canonical-form rewriter and **no formatter row exist in any skill**, so names live here or nowhere; operational-state directive cited change-data-capture rejection appearing **only in non-authoritative variant**, as converging instance, without saying it the foil — instance withdrawn, and its policy-engine ground come from hosting record outside declared source set, now named; two records **disagree within a day** on whether predecessor's function numbers are coverage checklist, skill published earlier framing under later record's date — both now carried, neither picked; review-substitute conjunction universalised where record scope it to money paths; *one idiom* dated to latest of its three sources; "weakest directive on enforcement" + "longest sustained argument" both superlatives no re-derivable, dropped; bus-factor directive's only premise-bearing clause is conversion's own addition, now said out loud rather than dressed as premise-derived; worked case **half-fail its own bus-factor directive** — one dependency priced, runtime framework in request path never — now stated, `backend-stack` precedent; and `backend-stack/SKILL.md` left stale while its `evidence.md` swept, **repo's worst recorded shape, inconsistent with same sweep reaching both `java-backend-rules` files** — now swept.

**What audit checked and found clean**, stated so blanks no read as coverage: discovery + frontmatter parse, structure, forbidden tokens, marker-word-as-plain-English, status-tier gloss word-for-word against `enforceable-rules`, nine cross-skill citations verified by opening cited rule, six re-derivable counts, source fidelity on exit ladder + non-quarantinable set + review inputs + "no threshold in any unit", three narrowing sweeps in `java-backend-api` + `money-java` with both files of former reached, `java-backend-observability` correctly left alone, `README.md` swept, `BACKLOG.md` row deleted with residues promoted. **Not checked**: the unread records in source repo, and whether every published skill hold sentence this publish should have swept — audit swept one term set.

