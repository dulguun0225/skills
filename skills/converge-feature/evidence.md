# Converge a feature — evidence

For a human deciding whether to trust the directive text. Every claim below is *convention* — a decision recorded with its date and observed ground, no research pass and no refutation panel behind it.

## Call build-feature's script; write no loop

Ground, 2026-09-18: feature `001-product-hierarchy` of a spec-kit Java backend (the product-catalog repo, `main`, head `f2b5f1f`), fully implemented and wall-green, converged by hand — `/speckit-converge`, then `/speckit-implement` on the appended phase, then the wall, repeated. Five passes. Each pass found something the previous implement had not (a gate the plan named and the code lacked, a request limit not on the wire, a pattern not on the wire, wording), and the fifth appended nothing only because the operator had stopped applying findings graded LOW. The rule the operator arrived at by running it — stop when nothing converge reports is above LOW, not when converge appends nothing — became the stop rule in `build-feature`'s script, with the round cap raised from three to six and reaching it reported rather than escalated. *(The floor is still the only stop test, but `LOW` is no longer the default: see the last entry of this file.)* Before that change the script would have exited `needs-human` at round three of this run.

Why a second skill rather than a paragraph in `build-feature`: the loop is invoked at a different moment — after implement, against work that exists, often on a feature `build-feature` never touched — and a skill fires on the moment its description names. The alternative was `/loop` with a retyped sentence; rejected the same day on the three grounds the directive states: an unwritten stop condition re-judged each tick, no journal and no resume and no per-stage tier because every tick re-enters the main context, and a sentence that is per feature and per project. The `/loop` sentence on this run was `Run /speckit-converge then /speckit-implement …`, and the operator ended it by judgment.

**2026-09-18, two corrections from an adversarial review of `cc903d0`.** Ground: a reading of the commit that created this skill, against `build-feature`'s own text, the same day it was written. **Nothing was executed — no run has been made through this skill, and the review is a reading, not a measurement.** First, the directive diagnosed the wrong cause for the failure a reader will actually hit. `build-feature` states that the Workflow tool accepts a `scriptPath` only from a directory the session may read, so a user-level install under `~/.claude/skills/` needs `/add-dir` or a `Read` allow rule first; this skill omitted that and told the reader an unreadable path means a missing sibling, with `npx skills add` as the fix — which does nothing for an install that is present but unreadable. The precondition is now stated here in the same terms, and the missing-sibling sentence is kept for its own case, which is real. Second, `push` and `mergeInto` were named without their defaults beside an example that runs on `branch: "main"`, so a reader following the example has finish push `main` directly with nothing on the page saying so. Both defaults are now inline, with the consequence for that example named. The stop rule this skill points at was corrected in the same pass and is stated in `build-feature`, not here: the severity floor is the loop's only stop test, a `converged` return carrying a finding above it ends the run `needs-human`, and the round cap is followed by one assess-only round whose findings are the ones the run carries.

Why no script of its own: `build-feature`'s `from` and `until` arguments already bound a run to any contiguous stage range, and a run starting after preflight already refuses to start without `args.wall` (the `state.wall = null` defect of 2026-09-17, recorded in that skill). Duplicating the loop would have created the second copy of a directive that this repo's write-once rule exists to prevent, and the two would have drifted at the first change to either. The sibling path `${CLAUDE_SKILL_DIR}/../build-feature/workflow.mjs` rests on the flat `skills/<name>/` layout both skills install into; a missing sibling fails at the Workflow call with the path it could not read, which is the loud failure the repo's rules require of a precondition.

**2026-09-18, the floor is chosen here.** *(Superseded in its default by the entry below.)* Ground: the owner asked that this skill be able to choose the severity floor, defaulting to `LOW`. The floor is `build-feature`'s argument and its check — `args.severityFloor`, validated against `HIGH`, `MEDIUM`, `LOW` (and now `NONE`) before the first agent starts, `CRITICAL` refused on a read of `/speckit-converge`'s Step 5, recorded in `build-feature`'s evidence file — and what changed here is where a reader is told about it: it is in the example call and stated as the one setting that moves where the loop stops, because this skill is the entry point invoked when a feature is already implemented and closing it out at `MEDIUM` rather than `LOW` is a per-feature call. Nothing was executed; the skill still has no run behind it.

**2026-09-18, the default floor is `NONE`.** Ground: the owner's decision, taken after being shown the counter-argument — the `001-product-hierarchy` run recorded at the head of this file, which is why `LOW` was chosen that morning — and reaffirmed. **Nothing was executed; no run has been made through this skill at any floor.** The rule and its check stay `build-feature`'s: `NONE` is a legal floor there, ranks below every graded severity, and is the default, so the loop tolerates no finding at all. What changed here is what a reader of this skill is told and what the example call shows — `severityFloor: "NONE"`, the legal set as `NONE`, `LOW`, `MEDIUM`, `HIGH`, and the frontmatter `description`, which said *until nothing converge reports is above LOW severity* and was false the moment the default moved. The description edit is the reason this entry names a cost: it invalidates any firing baseline, and this skill has never measured one.

The rejected alternative is the `LOW` default, and the ground it lost on is that a tolerated finding is a finding left open. The consequence for this skill's own use — closing out a feature that is already implemented — is the sharp one: a `converge-feature` run at the default will usually end at `maxConvergeRounds` with its open findings reported, or `needs-human` where converge reports converged while still grading a gap, rather than at a clean converge. A caller who wants the old behaviour passes `severityFloor: "LOW"`, which is why the floor is an argument and why this skill is where it is set.

## What this skill does not do

The scope boundary — converge to finish, nothing earlier — follows from what `/speckit-converge` does on a feature that is not implemented: it appends every unbuilt requirement as one phase, which is `/speckit-tasks` done badly. Cost is unmeasured: `build-feature`'s only end-to-end record put implement, converge and finish at about 2.2M subagent tokens for one converge round that reported converged; a run that takes several rounds at the floor now has one, dated 2026-09-22 at the foot of this file — three runs reached the six-round cap at floor `NONE` on 2026-09-20 for 2.25M to 2.47M subagent tokens and 49 to 60 minutes apiece.

**2026-09-18, a `converged` round with findings is no longer this skill's expected stopping place.** This skill's own text said to expect a run at the default floor to end at the round cap *or* `needs-human` where converge calls a round converged while still grading a gap. That second expectation is gone: `build-feature`'s loop now appends such a gap itself as a forced convergence round and implements it, and only a gap that survives its own forced round is a person's. Ground, dates and the rejected alternative are in `build-feature`'s evidence file — the loop, its stop rule and its exits are that skill's and are recorded once, there. The same applies to the reconcile-then-retry added behind a failed forced append on 2026-09-19: an exit this skill's text would otherwise have implied is a person's is the loop's, and it is recorded there. Unmeasured here as everywhere: no `converge-feature` run has taken the forced path, at any floor.

**2026-09-21, the arguments this skill passes changed under it, and one exit gained a third blocker.** Ground: the workflow change recorded in `build-feature`'s evidence file — features are specified by a domain expert with stock spec-kit, the script starts at `plan`, and no stage of it edits `spec.md`. **Nothing was executed here either; no run has been made through this skill at any floor.** Two consequences for this page. `featureDir` and `branch` are no longer things a caller must know: `build-feature`'s preflight discovers the feature on every entry, this skill's `from: "converge"` included, from `.specify/feature.json` or the branch name, so the example call is now the wall, the floor and the stage range, and the two arguments stay documented as the override a caller needs when the checkout is not on the feature. `args.wall` is unchanged and still required, for the reason it always was. And a forced convergence task may now be blocked by a third thing — a fix whose only route is an edit to the spec — which is the loop declining to rewrite the document it is judged against, not a route back to the written-reason closure removed on 2026-09-20. The base-branch refusal that arrived with discovery is a *full preflight's* refusal only: a converge run on a feature implemented on the trunk, which is how `001-product-hierarchy` was converged by hand, still starts.

**2026-09-21, later: what the shared preflight now does before this skill's loop starts.** Ground: the branch and sync work recorded in `build-feature`'s evidence file. **Unrun here as everywhere.** Three consequences for a converge run. It resolves the feature without trusting `.specify/feature.json` — which is git-ignored machine-local state — and then points that file at the feature it resolved, so `/speckit-converge` and `/speckit-implement` read the same directory this loop does; before that, a stale pointer would have sent both into the previous feature. It merges the base branch into the feature branch first, so a close-out is assessed against the spec as it stands rather than as it was. And a run on a feature implemented on the base branch still starts — no branch is made, nothing is merged — but it now leaves no `HANDOFF.md`, because that file is committed on the branch the run is on and this script does not commit to the trunk; the full report is on the return value instead. *(Superseded 2026-09-25, below.)*

**2026-09-22, a `needs-human` return from this skill's call is the session's to resolve.** Ground: the owner's decision, recorded with its run in `build-feature`'s evidence file, which owns the rule as it owns the loop's stop rule and its exits. **Nothing was executed here or there.** One consequence for this page, and it is the reason the rule appears in this skill's body at all rather than only as a pointer: a session that invokes `/converge-feature` never loads `build-feature`'s `SKILL.md`, so a rule about what *this* session does when *this* call returns has to be readable from here. That is the first deliberate duplication between these two skills, it is one paragraph, and `build-feature` is named as the owner beside it. Nothing about the loop, the exits or the stop rule moved: the script stops on exactly what it stopped on before, and what changed is who reads the stop first.

**2026-09-22, the first read of the run journals, and the two claims on this page it withdraws.** Twenty-eight Workflow journals existed for this loop's script — 2026-09-17..22, two service repositories, one machine, Claude Code CLI 2.1.278 — and none had been read back. The reader is `npm run runs` in the repository these skills distribute from (`https://github.com/dulguun0225/skills`, under `scripts/`), a report and not a gate; every figure here moves when it is re-run, which is why the command matters more than the number.

**Withdrawn: *no run has taken the forced round*.** Seventeen `force-append` dispatches over four runs on 2026-09-20 — `wf_3dc51822-772`, `wf_fbb96088-993`, `wf_c6f73cab-e1f`, `wf_1ee836c9-35f` — fifteen of which became an implemented `Convergence (forced round n)` phase with the wall green. The entry above already recorded the first of those runs in detail; the status line went on denying it, which is the shape worth naming: **the dated log grew and the summary did not.** `build-feature`'s bounded wall repairs and its reconcile behind a failed forced append stood in the same clause and have still been taken by no run, so the clause was wrong about one mechanism of three and right about two — which made the one with evidence read exactly like the two without it. *(Dated by the 2026-09-24 sweep of forty-nine journals: the wall repair has since run once, `wf_9e8bb81f-135`, 2026-09-22, and turned its phase green; the reconcile has still never run. The same sweep found the first clean `converged` at floor `NONE` on a run entered at `converge` — `wf_56a7b40b-9e7`, 2026-09-22 — so the round cap is the usual end of a run at the default floor, not the only one.)*

**Withdrawn as unanswerable rather than corrected: *no feature has yet been converged by invoking this skill*.** No journal names the invoking skill. `workflowName` is `build-feature` and `scriptPath` is its script on every run, so a `/converge-feature` call cannot be told from `build-feature` started with `from: "converge"`. Nine runs entered at `converge` — three reaching the round cap at floor `NONE` on 2026-09-20 (1.70M to 2.47M subagent tokens, 46 to 60 minutes), three returning `converged` at a floor deliberately raised to `LOW` or `MEDIUM` on 2026-09-18, and three taking the `needs-human`-on-a-converged-round exit that the forced round was written to remove. **Whether any of them came through this skill is not knowable from the record, and the page now says so instead of claiming the negative.** The cheap fix — an argument naming the invoking skill — was considered and declined by the owner the same day: it is a change to the measured system, and it would have been made before the first measurement was read.

**What the journals cannot say, so nothing on this page may claim it.** No cost in money: one undifferentiated token number per agent, no input/output/cache split. No skill names at all, so whether any directive skill reached an implement agent on a forced phase is invisible. No wall attempts inside an implement agent. Agent result previews capped at 401 characters, so a graded finding the script did not lift into its return value is gone. Tokens null on 26 of 276 agents, which is why every total here is a floor.

**And the general lesson, which is not about this skill.** The lapse rule downgrades a *confirmed* marker once its date passes, with no maintainer action needed. Nothing ever promoted an *unrun* claim when a run took it. **A claim that something has not happened is as perishable as a claim that it has, and it decays in the flattering direction** — it leaves an untested mechanism and a working one indistinguishable in the text a consumer actually loads.

**2026-09-25: a run on a feature implemented on the trunk now makes its feature branch, and a restart after a spec edit keeps the ticked tasks.** Ground: two decisions of the owner the same day, recorded in `build-feature`'s evidence file, both in the shared script. **Unrun here as everywhere; exercised against a stub of the Workflow sandbox.** Started on the base branch, this skill's run no longer converges and commits there: preflight refuses a dirty tree, makes the feature branch at the base branch's HEAD and checks it out, and the loop, its `HANDOFF.md` and finish's push are on that branch, so the base branch receives the work only through `mergeInto` or a person's merge. And the restart `from: "review-plan"` that every spec-change exit of this loop names no longer regenerates `tasks.md` over a converged feature: the tasks stage updates it in place, ticked work and forced convergence phases kept, changed work reopened with its reason, unneeded work struck through as removed, and a parse-only reader certifies no ticked task was lost. The sentence this skill carried on 2026-09-24 — that such a restart re-implements every phase — is withdrawn with it.

**2026-09-25, later: the base branch is discovered, and a missing `plan.md` or `tasks.md` makes no branch.** Ground: two decisions of the owner the same day, recorded in `build-feature`'s evidence file, both in the shared script. **Unrun here as everywhere; exercised against a stub of the Workflow sandbox.** This skill's example passes no `baseBranch`, so its runs take the discovered base: `origin/HEAD`, else the one local `main`, `master`, `develop` or `dev`, else a stop at preflight with nothing written. In the three service repositories as read that day none of those answers, so a close-out there stops until `baseBranch` is passed or `origin/HEAD` is recorded once per clone — where until then it took `main` and, started on `dev`, converged and committed on `dev`. Started on the base branch, a feature missing `plan.md` or `tasks.md` is now refused before the feature branch is made, and a feature branch ahead of the base is checked out rather than refused where the base holds neither file.
