# Build a feature — evidence

For a human deciding whether to trust the directive text. Every claim below is *convention* — a decision recorded with its date and observed ground, no research pass and no refutation panel behind it.

## Run the workflow; orchestrate nothing yourself

The run this skill is modelled on, 2026-09-16 to 2026-09-17: `specify workflow run speckit` driven from inside a Claude Code session for the `002-product` feature of a Java backend, with the session answering both gates after spawning a fresh-context Opus review of each artifact. Both reviews found blocking defects the writing context had passed — the constitution lagging the plan, a test-property precedence error, an ArchUnit scope error — and each was fixed in the artifact and committed before the gate was approved. Timings from the engine's own log: specify 19 minutes, plan 29, tasks 11, implement 53; the tasks step exited 1 on a transient spend-limit error after writing a complete `tasks.md`, and the run was resumed by patching the engine's state file. A later `/speckit-converge` appended eight gate and documentation gaps as a ninth phase, all implemented; the second converge is expected to report converged.

What spec-kit's engine (specify-cli 1.0.7) can and cannot do, read from its source the same day: a `command` step carries `model`, passed as `--model` to `claude -p "/speckit-<stem> <args>"`; per-step `integration_args` and `integration_options` exist in the schema but the Claude integration inherits the base `validate_runtime_config`, which raises on any value, so `--effort` can only reach the child through `SPECKIT_INTEGRATION_CLAUDE_EXTRA_ARGS`, one value for every step of the run. A `gate` step returns `PAUSED` when stdin is not a TTY. A `command` step's stdout is not captured ("a planned enhancement" in its docstring), so a `while` step cannot be conditioned on what converge reported. A `shell` step could run `claude -p` with any flags, but then the engine contributes nothing the script here does not.

Alternatives weighed and rejected, 2026-09-17: a Node script driving `claude -p --model --effort` per stage from a terminal — the same control flow, re-implementing the resume, journal and progress that the Workflow tool already has, and needing its own permission and environment handling for the child sessions; and one orchestrating agent that invokes the speckit skills in its own context — the human gate moved into the writer's context, which is the failure the two fresh reviews caught.

## The spec is the domain expert's, and no stage of this run edits it

Ground: the owner's decision of 2026-09-21, settled before the change was written. The team's workflow moved — a domain expert writes `specs/<NNN>-<name>/spec.md` inside the service project with stock `/speckit-specify` and `/speckit-clarify`, so the feature branch, the feature directory and `.specify/feature.json` exist before any unattended run starts, and the build begins at `/speckit-plan`. At the same time the service repos dropped the upstream-provenance layer of their traceability gate, so the documents the specify prompt used to pin, account for and cite are gone from those repos and from every prompt here. **Nothing was executed: no Workflow run has started from this shape.**

Two things follow, and the second is the one worth arguing. The first is arithmetic: the specify, clarify and spec-review stages, their fix agents, their tier rows, their round counter, their schemas and the two arguments that named the source document and the spec-kit short name are deleted rather than made optional, because a stage nobody enters is a second way to build a feature that no gate exercises. The second is that the spec becomes an input the run may read and not write. The alternative — let the fix agents keep editing it, since they are the same agents as before — was rejected on what the artifact is for: `spec.md` is now the only artifact in the run that no agent of the run authored, and that is the whole of its value as the oracle every later gate is judged against. An artifact the run may rewrite cannot refute the run. The cost is a new class of stop: a finding whose only remedy is a spec change ends the run instead of being absorbed, which is why the two fix rows carry `specChanges` and why the implement prompt admits a third blocker. That is the honest trade, not a gap — the run can close a gap in the plan, the tasks, the code and the gates, and a gap in the spec belongs to the person who wrote it.

The review that used to refute the spec against its source document is gone with the source, and the gate that remains has to keep an outside oracle: review-plan refutes `plan.md` against `spec.md`, the constitution and the code, none of which that stage wrote. Where a requirement makes every plan wrong, the reviewer is told to say so as a spec change rather than to propose a plan that contradicts the spec quietly.

## Each stage runs on the tier it earns

*(Two sentences of this summary are superseded. The roster of six, by the last entry of this file: since 2026-09-23 the roster is Opus at low, medium and high effort, and no other model. And "no run has yet recorded the token cost per stage", by the 2026-09-22 entry, which read per-agent tokens for every stage out of the run journals — on tiers that are no longer the table's.)* The Workflow tool's `agent()` takes `model` and `effort` per call (its authoring reference, read 2026-09-17), and the Agent tool's `model` accepts the aliases `haiku`, `sonnet`, `opus`, `fable`. The table is a decision from the shape of each stage's work, not a measurement: the reading-heavy stages return facts or findings and the writing stages produce artifacts a stronger tier reviews. The roster of six pairs (2026-09-18, below) is the owner's decision, the same six tiers the owner's agent roster runs on, so that a stage here is priced like the same work anywhere else. No run has yet recorded the token cost per stage; when one does, the numbers belong here with their date.

## A gate is a fresh-context refutation, not a human

Ground: the two gate reviews of 2026-09-16 above, each a fresh Opus context prompted to review, each finding blocking defects. The refutation framing and the findings-as-data return follow the Workflow tool's adversarial-verify pattern, where a verifier prompted to refute catches plausible-but-wrong artifacts that a verifier prompted to approve waves through. The round caps are a decision: two fix rounds for a spec or plan, because a third round on the same findings means the finding is contested and a person should read it; three converge rounds, because the one observed converge appended eight tasks and a second is expected to append none — a prediction refuted and the cap raised to six on 2026-09-18, recorded at the foot of this file.

## Analyze goes back to the artifact it names; converge goes back to implement

`/speckit-analyze` is read-only by its own text and ends by offering remediation edits to the user; `/speckit-converge` is append-only by its own text and reports `converged` or `tasks_appended`. Both give the loop a clean signal. Editing in place rather than re-running was decided on 2026-09-17 from the review-fix commits of the `002-product` run: each `/speckit-*` command regenerates its file whole, so a re-run would have discarded those fixes.

## Unattended means the agent decides by a written rule

The interactive points in the installed skills, read 2026-09-17: `/speckit-specify` presents up to three clarification questions and waits; `/speckit-clarify` is a one-question-at-a-time loop of up to five; `/speckit-implement` stops on an unchecked checklist and asks; `/speckit-analyze` asks whether to suggest remediation. Each had a rule in its stage prompt, and each rule wrote its decision into the artifact so the next review could refute it. The first two are no longer this script's to answer (2026-09-21): a person runs them before the run starts, and an unresolved `[NEEDS CLARIFICATION]` marker left in the spec is preflight's refusal rather than a question an agent answers for its author.

## What this skill does not do

The skill is inception-cadence for a feature — it fires once per feature, and its frontmatter is paid every session; it exists because the alternative was a person at two gates and a night of attention per feature. The sequential shape is a decision: spec, plan, tasks and code are one dependency chain, and the one place work could fan out — implement phases — is kept sequential because each phase's wall run touches the same tree.


## 2026-09-17 — first end-to-end run, product-catalog 003-product-version

Run `wf_cc1aa65d-148`: preflight, specify, clarify (5 questions self-answered), three spec-review rounds (6 → 3 → 1 blocking) in 71 min, 8 agents, 1.19M subagent tokens; exit `needs-human` at the spec review. The remaining blocker was a constitution decision (three Article VII articles written by 002 about tables 003 owns). Consequence: the admission test in the plan, review-plan and fix-plan prompts, and in the project's constitution Governance.

**2026-09-17, tier ceiling lowered to Opus.** Runs `wf_cc1aa65d-148` and `wf_e460bffc-289` (Fable high on the reviews, xhigh on plan) cost 1.19M and 0.91M subagent tokens for eight and five agents without leaving the spec review. The user's verdict: the tier eats too many tokens for what it adds; every `fable` row in `TIERS` is now `opus`, efforts unchanged.

**2026-09-17, first feature built end to end (product-catalog 003-product-version).** Seven launches: three ended `needs-human` at the spec review, one at review-plan, one at analyze, each at the round cap with one or two fresh blocking findings, most of them holes opened by the previous round's fix; each time one fix agent applied the open findings and the run restarted at the next stage. Implement ran ten phases, every wall green, 103 tasks; converge reported converged in one round with zero findings. About 8.5M subagent tokens, of which 6.3M were spent before any code (specify through analyze, with their review and fix rounds) and 2.2M on implement, converge and finish. Defect found: a run started after preflight had `state.wall = null`, every prompt said run `null`, and finish reported `wallGreen: false` on a green branch; the script now refuses such a start without `args.wall`. Open question, not decided: a review round with zero blocking findings could apply its majors once and proceed, as it does for minors.

**2026-09-18, the roster.** *(Superseded by the last entry of this file: the six-pair roster below is gone, and no stage runs on Haiku, Sonnet or Fable since 2026-09-23. The record of what this entry decided and why stands.)* The owner set the tiers this script may use to six pairs — Haiku, Sonnet 5 low, Sonnet 5 medium, Opus 5 low, Opus 5 medium, Fable 5.1 low — not all of which need a stage. The rows outside it were Opus high (the spec review, review-plan, converge) and Opus xhigh (plan). Plan and the two artifact reviews went to Fable low, the roster's tier for an architecture decision and an adversarial review; converge went to Opus medium, because it is the most reading-heavy stage, runs up to three rounds, and its output is implemented and wall-checked rather than trusted. Every other row is unchanged. `tier()` now refuses a row outside the roster before the first agent starts. Unmeasured: the 2026-09-17 token record that lowered the ceiling was taken with Fable at high and xhigh; the next end-to-end run on this table is the first measurement of Fable at low, and the row to watch is the spec review, where both earlier runs spent their tokens.

**2026-09-18, converge stops at a severity floor.** *(Superseded in its default by the last entry of this file: the floor is now an argument defaulting to `NONE`. The ground below stands as the record of why `LOW` was chosen.)* Ground: feature `001-product-hierarchy` of the same product-catalog repo, converge and implement driven by hand on `main`, head `f2b5f1f`. Converge had no fixed point: five passes, each finding something the previous implement had not, and the fifth appended nothing only because the operator had stopped applying findings graded LOW — wording, naming, a gap a person would not notice. The operator's rule, arrived at by running it, was to stop when nothing converge reports is above LOW severity, not when converge appends nothing. Consequences in the script: the converge schema carries `findings[].severity` on the four-value scale the analyze schema already uses (a second vocabulary was not admitted, so the two stages' logs read on one scale); the loop ends on `converged` or on a round with nothing above LOW, and logs which; `maxConvergeRounds` is 6, and reaching it is `converge.ended: "round-cap"` on a `done` return rather than a `needs-human` exit, because the wall is the gate and the remaining rounds at the floor are noise. A red wall after an appended phase and an unchecked appended task stay `needs-human`. The evidence for three rounds recorded above — *a second is expected to append none* — was the prediction this run refuted. Unmeasured: how often the floor ends the loop before the cap; the first scripted run to reach converge on this table is the measurement.

**2026-09-18, the severity-floor commit reviewed and corrected.** *(Its six corrections all stand. One consequence it states — that an empty grade on the assess-only round ends the run at the floor — is now labelled `converged` when the floor is `NONE`; see the last entry of this file.)* Ground: an adversarial review of `cc903d0` — the commit recorded directly above — read against `/speckit-converge`'s own text, in the same repo, the same day. **Nothing here was executed: no Workflow run has reached the converge stage on this table, and the review is a reading of the script and the skill, not a measurement.** Eight defects, all landed in one commit: the six that bear on this script are below, and the two that bear on `converge-feature` — the install-failure diagnosis and the undeclared `push` and `mergeInto` defaults — are in that skill's evidence file. (1) The loop consulted `outcome` before the floor, and `/speckit-converge` Step 7 defines `converged` as *no **actionable** findings* while Step 4 surfaces `unrequested` gaps for awareness only — so a HIGH gap the round judged non-actionable arrived as `converged` and broke the loop before the floor was read. The floor is now the only stop test, and a `converged` return carrying a finding above it is a contradiction rather than a work state: nothing was appended, so the next round would repeat it to the cap, and the run ends `needs-human` with those findings. (2) The converge prompt had written a third severity vocabulary beside the skill's Step 5 and the analyze schema's, and its LOW clause — *a gap a person would not notice* — graded exactly the class the hand-driven run kept finding, a missing request limit on the wire. The four ad-hoc clauses are gone; the prompt asks for every gap the assessment found, appended or not, graded by Step 5 and by nothing else. The sentences above and in `SKILL.md` calling that scale `/speckit-analyze`'s were wrong in the same way: it is the converge skill's own, and the two agree on their four values. (3) At the cap the findings the run carried were the ones that round had just closed, because every `tasks_appended` round implements its phase before the loop re-checks; the log line *with findings above LOW still reported* was false. The cap is now followed by one assess-only round — the same prompt, appending nothing, committing nothing — the shape the review and analyze loops already run as `max + 1`; its findings are what `state.converge.findings` carries, and an empty grade there ends the run at the floor rather than at the cap. (4) `findings: []` satisfied the schema's `required`, so a round that appended tasks and graded nothing stopped at the floor claiming nothing was above it when the truth was unknown; that shape now counts as above the floor and the loop continues. (5) `SEVERITY_FLOOR` was compared with `!==`, which would break silently the day the floor is raised to MEDIUM; the comparison is by rank against the declared order, and a severity off the scale ranks above the floor. (6) The `status: "done"` check claimed the finish wall is green, while an `until` before finish returns `done` with `finish: null`; and *no third exit* was a claim about every way the script can stop, where a throw from `must()` or the argument checks is neither return. Both corrected in place.

**What the correction does not decide.** Whether a converge agent returns findings at all, or grades them the way Step 5 says, is unmeasured — every path above was exercised against a stub of the Workflow sandbox, not against an agent. The assess-only round costs one more converge-tier agent on any run that reaches the cap, and that cost has no number. Whether the floor or the cap ends a real run is still the first scripted run's measurement. **All three were answered by the 2026-09-22 sweep at the foot of this file, and the answers are the predicted ones.** Converge agents do return graded findings — 188 of them reached `converge.forced` across the corpus, all at `LOW` on the four runs that forced anything. The assess-only round has a number: four agents, 550,863 subagent tokens, a mean near 137,700. And the cap ends a real run, not the floor: of seven runs whose converge loop terminated, four ended `round-cap` and three `converged` only at a floor deliberately raised to `LOW` or `MEDIUM` — **at the default floor `NONE` no run has ever ended `converged`**, which is what this skill said would happen and is worth reading as a confirmed prediction rather than as a defect. *(Dated by the 2026-09-24 sweep: three runs have since ended `converged` at `NONE` — `wf_56a7b40b-9e7`, `wf_fb809526-d13`, `wf_e4af9888-80a`, 2026-09-22..23 — against five at the round cap, so the cap is the usual end at the default floor and not the only one.)*

**2026-09-18, the floor became an argument.** *(Superseded in its default by the last entry of this file: `LOW` is no longer the default. Everything else here — the rank comparison, the prompt isolation, the `CRITICAL` refusal — still holds.)* Ground: the owner asked for the converge stop level to be chosen per run, with `LOW` the default, from both entry points. **A shape decision, not a measurement — nothing was executed, and no scripted run has yet reached the converge stage.** `SEVERITY_ORDER` moved to module scope beside `ROSTER`, `args.severityFloor` joins the configuration uppercased with `LOW` as its default, and a value off the four-value scale throws beside the tier check, before the first agent starts — the same rule the roster gets, for the same reason: a bad floor should not cost a run its specify-through-implement spend before it is caught. The rank comparison recorded as defect (5) above is what made this a three-line change rather than a rewrite; it was written for *the day the floor is raised to MEDIUM*, and this is that day. Two properties worth keeping: the floor is filtered in the script and never enters the converge prompt, so moving it cannot move a grade; and `converge.floor` on the return already carried the value, so a run's stop level is readable from its result without knowing what the default was when it ran. `CRITICAL` was accepted as a floor for one edit and then refused the same day, on a read of `/speckit-converge`'s Step 5 rather than on an argument: that step grades a constitution MUST violation, or a gap blocking a P1 user story's baseline functionality, as CRITICAL — so a floor there tolerates every finding the scale has and ends the loop after one round. The legal floors are `HIGH`, `MEDIUM`, `LOW`, and `CRITICAL` throws with a message saying what it would have meant. **The first version accepted it because the scale was taken from this script's own schema rather than from the skill that grades against it — the enum was checked, the definitions behind it were not**, which is *follow the pointer* in its usual direction: a claim about another file's contents, written without opening the file. Unmeasured, as before: how often any floor ends the loop before the cap.

**2026-09-18, the default floor reversed to `NONE`.** Ground: the owner's decision, taken after being shown the counter-argument and reaffirming it. **No run: nothing here was executed, and the skill stays *decided, not yet validated*.** The entry directly above records the floor becoming an argument with `LOW` as its default, and the entry before that records the `001-product-hierarchy` run that argued for `LOW` — that converge has no fixed point, and that the only thing which ended the hand-driven run was the operator ceasing to apply findings graded LOW. Both entries stand as the dated record of what was decided and why; **this one supersedes their default.** `NONE` joins `HIGH`, `MEDIUM`, `LOW` as a legal floor and is the default, ranking below every graded severity (`floorRank = SEVERITY_ORDER.length`) so that every graded finding is above it; `CRITICAL` is still refused on the same Step 5 reading. The rank comparison and the off-scale rule (`rank === -1` is above the floor) are unchanged, which is again why this was a small change: the floor was never compared by equality.

The rejected alternative is the `LOW` default this skill shipped for part of the same day, and the ground it lost on is the owner's: **a tolerated finding is a finding left open, and a loop whose stop test tolerates a class of finding has written that class out of the definition of done.** The no-fixed-point evidence was weighed against it and did not carry: what that run shows is that the loop will not terminate on its own, not that a tolerated finding is closed. The cost is accepted rather than argued away — under `NONE` the clean `converged` exit needs a round that appends nothing *and* grades nothing, so a real feature will usually end at `maxConvergeRounds` (unchanged at 6; a caller who wants more passes raises the cap, not the floor) with its open findings reported on a `done` return, or at `needs-human` where converge returns `converged` while still grading a gap. That `needs-human` was already the contradiction branch and is now much more likely, because `/speckit-converge` Step 7 calls a round converged on *no **actionable** findings* and Step 4 surfaces gaps for awareness: under `NONE` the run declines to take that skill's non-actionable judgment as its own.

Two exits changed meaning rather than code. The in-loop `severity-floor` exit is unreachable under `NONE` — a round that grades nothing already continues as *shown nothing*, and any round that grades something has graded above the floor — so it is now the exit of a run that raised the floor, and says so in a comment. On the post-cap assess-only path a zero-finding assessment previously ended `severity-floor`; under `NONE` it is labelled `converged`, because the round found nothing and a label naming a floor that tolerates nothing would describe the wrong thing. Every log line and the contradiction message that named the floor are floor-aware, so no run prints *nothing above NONE*.

Exercised, not measured: the seven exits above were driven against a stub of the Workflow sandbox — `NONE` with a clean round, with one LOW on a `converged` return, with an off-scale severity, the cap with a clean and with a graded assess-only round, and the same cap shapes at a `LOW` floor — and each produced the `converge.ended` and the log line this entry claims. That is a reading of the script under stubs and not a run: no Workflow run has reached the converge stage at any floor.

**2026-09-18, a needs-human exit reached only the session that started it.** Ground: run `wf_7e4e8726-4b5` on feature `specs/002-product` of the product-catalog repo, converge stage, exited `status: needs-human` with three findings. The payload existed in exactly three places, all machine-local and none of them in the repository — the invoking session's tool result, a file under `/tmp`, and the run journal under `~/.claude/projects/<slug>/<session>/workflows/` — so a colleague had no way to reach it. `grep -n 'writeFile' workflow.mjs` found nothing: `needsHuman()` constructed a return object and did no more. **Observed, then fixed; the fix itself is not measured — no Workflow run has yet taken the new path.**

The fix is additive and touches no stop rule: `needsHuman()` became `async`, its eleven call sites became `return await needsHuman(...)`, and the exit now dispatches one `handoff` agent (a new `TIERS` row, Sonnet low) that writes `HANDOFF.md` into the feature directory, commits it by pathspec and pushes it when `args.push` is true. Three decisions worth their reasons. (1) The document is rendered in the script and handed to the agent finished, with `{{RUN_DATE}}` and `{{RUN_JOURNAL}}` as its only substitutions — the script has no `Date` and does not know its own run id — so the cheapest tier in the table cannot paraphrase a finding or silently drop one; that is also why the tier can be the cheapest. (2) A preflight exit writes nothing, because there is no feature directory and no feature branch, and because a dirty tree is one of the things preflight refuses on, so a commit at the repo root would sweep uncommitted work into a handoff commit; the return says so under `handoff.note` instead. (3) The dispatch is deliberately *not* wrapped in `run()`/`must()`, which throw on a dead agent: a handoff that fails, throws or returns nothing returns a `written: false` record and the full needs-human payload goes back unchanged, because losing the findings because a write failed is strictly worse than the behaviour this replaced. The `finish` red-wall return — the one needs-human exit that never went through `needsHuman()` — writes the same file and keeps its own shape.

Exercised, not run: the patched script was driven against a stub of the Workflow sandbox over eight shapes — a preflight refusal (no agent dispatched, note explains why), the converge contradiction exit at floor `NONE` (one handoff agent, on Sonnet low, findings on both the file and the return), a handoff agent that throws, one that returns null, a run at `push: false` (no `git push` anywhere in the prompt), a red finish wall, a green finish (no agent at all), and an implement red wall whose `{unchecked, wallOutput}` detail renders. Each produced the return value and the document this entry claims. That is a reading of the script under stubs and not a run.

**2026-09-18, the converge loop can author its own repair; a `converged`-with-findings round no longer stops it.** Ground: run `wf_7e4e8726-4b5`'s successor handoff on feature `specs/002-product` of the product-catalog repo. The converge stage exited `needs-human` at floor `NONE` on four findings, all graded LOW — FR traceability citations, one `docs/GATES.md` table row described too thinly, two test and naming wordings — and every one of them was a mechanical edit the run's own implement agent could have made. **Observed, then fixed; the fix is not measured — no Workflow run has taken the forced round, at any floor.**

The defect was structural, not a mis-set threshold. The loop had exactly two moves: converge appends tasks and implement runs them. It could not author work itself, so the only thing it could do with a finding converge had graded and declined to append was hand it to a person — and the comment beside that branch already admitted the trap: *converge judging a gap non-actionable is exactly the judgment this floor refuses to make on its behalf.* Under the `NONE` default, where every graded finding is above the floor, that branch fires on any finding at all, so the shipped default dead-ended on cosmetics. The coherent reading of the floor is that a run which declines converge's non-actionability judgment owes the work that judgment withheld.

So the loop has a third move. On a `converged` return carrying findings above the floor it dispatches one `force-append` agent (a new `TIERS` row, Opus medium) with those findings rendered by the script and no mention of the floor; the agent appends `## Phase N: Convergence (forced round n)` to `tasks.md` in `/speckit-converge`'s own append format, one task per finding, commits `tasks: forced convergence round n`, and the phase then goes through the same `implementPhase` and the same wall gate as an ordinary appended round. The row is priced with converge and implement rather than with `handoff`, and the difference is the reason: `handoff` is handed a finished document and can only substitute two placeholders, where this agent is handed findings and must author the task text and judge per finding which closure route it leads with.

Four decisions worth their reasons. (1) **A forced task is closable two ways and its own text says both** — fix the defect, or record in `<featureDir>/convergence-rationale.md` why it is not a defect, with the rationale closing the task only once it is in the file. This mirrors what the handoff document already asked a person to do, and it is not a softening: in the `002-product` case FR-034 and FR-035 are requirements about *absences* — this capability contains no approval gate, no user-permission check lives here — and a task demanding a code citation for an absence invites a fabricated one, which is worse than the open finding. (2) **A finding is forced at most once**, keyed on severity, location and summary normalised, tracked across rounds and returned as `converge.forced`; converge has no fixed point (the `001-product-hierarchy` entry above) and each pass perturbs the tree the next one reads, so an unbounded force would spin. The identity is exact-match, so a finding the next round re-words is a new identity — stated rather than hidden, because the bound that actually holds is `maxConvergeRounds` and the identity only decides when the loop may claim it already tried. (3) **A survivor still escalates and says so**: the `needs-human` `why` records that the loop appended a task against this finding and implemented it with the wall green, and the assessment reports it again. A person reading a handoff must know the difference between nobody having tried and the loop having tried and failed. (4) **Round accounting**: the forced round runs inside the slot of the round that discovered it, and the loop then re-assesses exactly as after an appended round, so `maxConvergeRounds` — still 6 — remains the single bound on the stage's cost and a pathological run cannot spin. The extra price of a forced round over an appended one is one `force-append` agent.

The `converged`-with-findings escalation is the rejected alternative, and it is what this skill shipped from `cc903d0` on 2026-09-18 until this change the same day. The ground it lost on is the `002-product` handoff: it asked a person to *decide whether it is real, and either apply the fix or record why it is not a defect* — both of which the run's own agents can do, on findings the script had already filtered. What it got right, and what is kept, is that a finding nothing has closed must not be silently tolerated; the forced round closes it instead of tolerating it or delegating it.

Exercised, not run: every path out of the converge loop was walked by hand and then driven against a stub of the Workflow sandbox — converged-clean, converged-with-findings first time, converged-with-findings already forced, a re-worded finding (forced again, by design, and bounded by the cap), an off-scale severity, a forced append that reports `appended: false`, a red wall and an unchecked task after a forced phase, `tasks_appended`, `findings: []`, the in-loop severity-floor exit, a forced round in the cap's last slot, and the assess-only round after the cap. Each reaches exactly one terminus, the `force-append` agent's prompt carries the findings verbatim and no mention of the floor, and no finding is forced twice on one identity. That is a reading of the script under stubs and not a run: no Workflow run has taken the forced path.

**2026-09-18, every needs-human exit audited against one rule: a run must not hand back what it could have fixed itself.** Ground: the owner's rule, given while the forced convergence round above was being written — *"I better not find a 'requires-human' thing when the AI could have fixed it on its own."* All ten `needsHuman` call sites and the `finish` red-wall return were sorted into three buckets: **A**, the loop never tried and an agent could do the work — fixed by giving the loop the move, never by escalating; **B**, the loop tried and the attempt did not take — kept, with the `why` now stating what was attempted and how often; **C**, not the run's call — kept unchanged. **Nothing here was executed either: the paths were traced by hand.**

Bucket A, three fixes. (1) The `converged`-with-findings exit, above. (2) A red wall after an implemented phase escalated on the first agent's verdict; it now gets one fresh-context repair pass carrying the failing output, and only then escalates. The ground is this skill's own premise: the first agent had already run the wall up to `maxWallAttempts` times inside its own context, so another attempt by that context is not what was missing — a second context reading the same failure is, which is why every review gate here is a fresh context. (3) A convergence phase with tasks left unchecked escalated after one pass, where the implement stage had run a second pass over exactly those ids since it was written; both now go through one helper, so the two stages cannot drift again. The ceiling is three implement agents per phase — the phase, one wall repair, one pass over the unchecked ids — with no third shape and no retry of a retry, because an unbounded retry is a spin on the owner's budget and not a fix.

The red wall at `finish` is bucket A with a constraint. The loop turned that same wall green after every phase with the same agents, so a wall that was green at the last phase and red at close-out is a defect and not a decision. It gets one repair pass, **and the re-check is a second `finish` agent rather than the repairing agent's own word — an agent that repairs a gate is not the one that may declare it green.** That separation is the whole safety of the path, and it is why the repair agent is told its verdict is not final.

**No gate is weakened to make a wall pass, and the prohibition is enumerated rather than implied.** The repair prompts name the moves that are forbidden — skipping, disabling, quarantining or deleting a test; a suppression, exclusion, baseline entry, waiver or ignore comment; a lowered threshold or coverage figure; a relaxed, reordered or removed gate; an edit to the wall script, the build file or any gate configuration; a force flag — and instruct the agent that where the only route it can see is one of those, it changes nothing, leaves the tree as it found it and returns the wall red. A green wall bought that way is strictly worse than the escalation it replaces. This is the one place the owner's rule and the definition of done could be set against each other, and the rule loses: *could have fixed it* does not include *could have hidden it*.

Bucket B, kept with the caps raised once. `maxReviewRounds` and `maxAnalyzeRounds` go from 2 to 3. The 2026-09-17 record above is seven launches of which five ended `needs-human` at a review or analyze round cap, and the operator's resolution every single time was to run one more fix agent over the open findings — the exact round the cap had refused, which makes a cap of 2 bucket A wearing bucket B's clothes. 3 is also the largest increment that record supports: it says most findings at a cap were holes the previous round's fix opened, so these loops have no fixed point either and a further cap buys rounds rather than closure, at a stage where two runs spent 1.19M and 0.91M subagent tokens without leaving the spec review. What the cap must not become is a way to proceed on an unclean review: the gate is a fresh-context refutation, and *apply the findings and move on* — which is what the operator did by hand — would delete the gate rather than raise the cap. That remains a person's call and stays an escalation. *(The owner made that call on 2026-09-24, and made it the other way, with the survivor half kept: see the last entry of this file.)* **Measured since, and the raise did not buy closure: six runs took a third round — four at review-plan, one at review-spec, one at analyze, each dispatching exactly three fix or remediate agents — and all six still ended `needs-human` at the cap** (2026-09-19 to 2026-09-22; re-read with `npm run runs`). So the prediction in this paragraph holds and the hope beside it does not: a bigger cap buys rounds rather than closure, and at three rounds the loop has bought every round the record supports. **What is still unmeasured is the counterfactual** — nobody has run a fourth round on one of those six to see whether it would have closed, which is the one measurement that would settle whether 3 is a cap or a coincidence. *(Still untaken on 2026-09-24, though it looked taken: the launching session applied the fourth review's findings on seven review-plan stops and restarted `from: "review-plan"`, and that start ran no review until the same day — see the last entry of this file — so no fifth review ever read what was applied.)*

Exercised, not run: the audit's paths were driven against the same stub — an implement phase whose wall stays red (two implement agents, the second labelled *wall repair*, carrying the first's failing output and the enumerated prohibition), a forced phase whose tasks stay unchecked (two passes over exactly those ids), a review cap (four reviews, three fixes, and a `why` naming both counts), a red finish wall repaired and re-verified by a second `finish` agent, a finish wall still red after that (the handoff says the loop tried twice), and a preflight refusal (unchanged, no agent dispatched). Seventeen shapes, fifty-nine assertions, all green. Still a stub and not a run.

Bucket C, kept unchanged, and the honest list of what still stops a run. Preflight's two exits: a repository it refuses on — a dirty tree, the wrong branch, a missing speckit skill, a source path that does not resolve — and no definition-of-done command. Acting on any of them means committing or discarding somebody's uncommitted work, switching branches, installing a skill, or guessing what the owner meant by a `wall` they did not pass; the owner's rule says *could have fixed it*, not *could have guessed*, and a wrong guess here destroys work. Beside them, the bucket B exits that stay: a review or analyze cap with findings open after three fix rounds and four reviews; a wall still red after the repair pass; a task two implement passes decline to tick; a converge finding that survives its own forced round; and a forced append that reports it failed to write `tasks.md` — attempted once and not retried, because a second attempt against a file the first may have partly written would duplicate the phase. Six real escalations, each one either something the loop attempted and failed at or something it is not authorised to do.

**2026-09-19, the one site that survived the escalation audit did not survive a second reading: a failed forced append is reconciled, not escalated.** Ground: the owner's rule of the day before, put a third time — *"Something that actually requires human is OK. Things that AI could fix and it calls for human — THAT'S A BIG NO!"* — and turned on the audit's own output. The entry above filed a `force-append` agent reporting `appended: false` as bucket B, kept it, and justified it like this: *it is attempted once and not retried, an agent that reports a failed append may have written part of the phase first, and a second attempt against that file would duplicate it — tasks.md needs a look before the run restarts.* The corruption risk is real. The conclusion was not: **that is an argument against a blind retry, not against a retry**, and *tasks.md needs a look* is the tell — every other bucket-A site in that audit had the same shape, something went wrong and the state was unclear, and each was fixed by a fresh-context agent that looks and then acts. Reclassified B → A. **Nothing here was run; stubbed only.**

What makes it reconcilable is that the file has an oracle. At the moment a forced append fails, nothing else in the run has touched `tasks.md` since the previous round's implement committed, so an uncommitted change to it is that agent's own partial write, `git status`, `git diff` and `git log` say exactly what it did, and `git checkout -- tasks.md` restores the committed file byte for byte. The earlier reasoning treated the file as opaque prose to be judged by eye; it is a tracked file with a history, and that is the difference between a guess and a reading.

So: one `reconcileTasks` agent (a new `TIERS` row, Opus medium) reads that evidence and brings the file to one of two states — the forced phase **wholly absent** or **wholly present and well-formed** — with nothing partial surviving. Then the parse-only `phases` row re-reads the file and certifies the verdict, because the agent that cleans up is not the one that may certify the result; that is the finish-wall-repair separation reused, and where the two disagree the loop stops rather than act on either account. Wholly present means the first agent wrote the phase and failed only to say so, and the loop implements it — re-appending would be exactly the duplication the old escalation was guarding against. Wholly absent means one retry of the append. Exactly one reconcile and one retry, no retry of a retry, and the forced-once identity rule still decides which findings may be forced at all. The reconcile writes to `tasks.md` and no other file, never ticks or unticks a task, never touches a phase other than this round's forced one, and runs no build and no hook.

The row is priced Opus medium for two reasons that are not the reading: it may delete lines from `tasks.md`, where a wrong deletion removes real tasks and nothing downstream would notice, and it must answer `unsure` rather than guess — a calibration judgment the cheap tiers are worst at. `unsure` is a legitimate escalation and the prompt says so in those words, because acting on a guess about this file either deletes real tasks or leaves a duplicate phase and neither is recoverable; the `why` then carries the reconcile's quoted evidence, so the person is told what is in the file rather than sent to find out.

**A correction to the entry above, which miscounted itself.** It claimed six real escalations and then listed seven. Counted as distinct reasons rather than as call sites, there are now **eight, across thirteen exits**: preflight refusing on the repository state; no definition-of-done command; a review or analyze cap after three fix rounds and four reviews; a wall still red after its one fresh-context repair pass, at a phase or at finish; a task two implement passes decline to tick; a converge finding that survives its own forced round; a reconcile that cannot establish `tasks.md`'s state or that the parse-only reader contradicts; and a forced append whose reconciled retry failed as well. Two of those eight are bucket C, four are bucket B, and two — the reconcile's `unsure` and the reconcile-versus-reader disagreement — are C for a reason worth naming: they are not *the run cannot do this*, they are *the run cannot establish which of two irreversible acts is the right one*, which is the same category as a dirty tree.

Exercised, not run: the stub gained the five paths this entry adds — the reconcile finding nothing and the retry succeeding, the reconcile finding the whole phase and the loop implementing it without appending again, a partial cleaned and the retry failing anyway, an `unsure` verdict (no certification read, no retry, nothing changed), and a reconcile the parse-only reader contradicts in both directions, including a phase it calls present whose tasks the reader finds already ticked. Twenty-two shapes, eighty-five assertions, all green. Still a stub and not a run: no Workflow run has taken any path added on 2026-09-18 or 2026-09-19. **Dated by the 2026-09-22 sweep at the foot of this file, and half of it is now wrong**: the forced convergence round of 2026-09-18 has run seventeen times, while the wall repairs of the same day and this entry's reconcile have still been taken by no run. *(And by the 2026-09-24 sweep: the wall repair has run once — `wf_9e8bb81f-135`, phase 2, red after three attempts, green after one fresh-context repair — and the reconcile still never.)*

**2026-09-20, the first scripted converge run at floor `NONE`, and the silent fallback it found.** `product-catalog` `004-product-gl-config`, `/converge-feature`, run `wf_3dc51822-772`: 26 agents, none errored, 2.32M subagent tokens, 61 minutes, six rounds, ended `round-cap`, wall green at every phase and at finish, pushed. Every round was a forced round — converge called each one converged and graded 12 to 16 LOW findings, the same standing set of named gaps each time, each closed by a dated written reason in `convergence-rationale.md` and re-raised by the next assessment. That is the measurement `converge-feature` said it lacked, and it is the predicted shape: at `NONE` the run buys six rounds of re-adjudication of gaps the feature's own artifacts already name as not closable here. The defect: in round 2 the forced append wrote phase 31 (T219–T231) and said so, the parse-only reader (Sonnet low) returned 13 of the file's 31 phases, and `phases.find(p => p.number === fa.phase) || phases[phases.length - 1]` handed implement phase 13 — `Convergence (round 2)`, one task, long since ticked. Implement reported it complete, wall green, and the loop went on. Phase 31's thirteen boxes stayed open to finish (`allTasksChecked: false`), and rounds 4, 5 and 6 each graded *Phase 31's unchecked boxes* as a finding and closed it by written reason, because converge is append-only and cannot tick a box. A fallback that was written for an append that names no phase number fired on a reader that stopped short, and nothing downstream could tell. Corrected the same day: `readAppendedPhase` finds the phase by its number or not at all, re-reads once with a prompt that names the miss and has the reader count the headings first, and exits `needs-human` with the phase and its task ids if the second read misses too; where an append names no number, the last phase stands in only while it holds unchecked tasks. Both sites — after a forced append and after converge's own — use it. Exercised by syntax check only; no run has taken the re-read. The needs-human conditions are now nine.

**2026-09-20, the forced round lost its second closure route, and the survivor check got a reader that can see a survivor.** The run above is the evidence. Two hundred and eighty-eight tasks in, 004's six forced rounds had changed no line of code: all of T206–T288 were closed by a dated entry in `convergence-rationale.md`, and the next assessment re-raised every one, because a written reason changes nothing converge reads the code against. The owner's reading the same day — *severity floor none means low must be fixed* — is the rule the forced round was written to serve and the two-route task defeated: the force-append prompt told its agent to *lead with the rationale route* for a finding about an absence, the agents classed every finding as an absence, and the implementer took the route that costs nothing. Four of the thirteen were then fixed in code in one 25-minute deep-worker pass with the rationale route forbidden — the status-write ban made class-scoped (`73c8092`), the service connected as a role the RLS policies bind (`42e443a`), the GL licences given a statement kind and a call site (`92e7e95`), the operation pin made to sweep the whole document (`7b70ebd`) — each of which six rounds had recorded as *not writable here*, *a deployment decision*, *not expressible on this toolchain*. A fifth, the stale Notes line, had been adjudicated twelve times since T124 because converge is append-only; it was one `sed`. So the route is removed rather than narrowed: the task text names the work, the implement prompt on a forced phase admits two blockers (a requirement or article that forbids the change, quoted; a system outside the repository that does not exist), names the four excuses above as not blockers, and a blocked task stays unticked and comes back in `blocked`. The exit for it is the one that already existed — a task two passes decline to tick — now carrying the blockers and saying that every other task was fixed. The second defect is why this cost six rounds instead of one: *a finding is forced at most once* keyed on exact normalised text, a fresh agent re-types every finding every round, and the survivor branch never fired. The assessment is now handed the numbered list of what the run forced and returns `repeatOf` per finding; exact match still counts. The label is asked for after the assessment and the floor is still never in the prompt. Not decided here: what a person does with a blocked finding so that the next run does not raise it again — 004 has six (the NC-14 contract, the broker, the P&L class checks, VII.3 against FR-044, `gl_config_complete`'s data-level half, the removal migration's guard), and until the spec or an owner-written record settles them a `NONE` run on 004 ends `needs-human` in its first round with those six named, which is the honest end. Exercised by syntax check only. The needs-human conditions stay nine: the blocked exit is the unticked-task exit with more in its detail.

**2026-09-21, the run starts at plan, and the spec is an input it may not write.** Ground: the owner's settled decision, recorded in the section above. The team specifies features by hand now — a domain expert runs `/speckit-specify` and `/speckit-clarify` in the service project and writes `spec.md` himself — and the service repos dropped the upstream-provenance layer of their traceability gate on the same day, so the pinned snapshots, the provenance rows and the script that moved them no longer exist to be cited. **Nothing was executed; no Workflow run has started from this shape, and the *decided, not yet validated* tier is re-earned rather than inherited.**

What was deleted, and why nothing replaced it: three stages (specify, clarify and the spec review), their fix and review tier rows, the spec-review round counter, two schemas, the two arguments that named the source document and the spec-kit short name, and every provenance sentence in the plan, tasks and implement prompts — the accounting ones only; the waiver rules they were interleaved with stay word for word, including that the tasks stage is the only place a waiver row may originate. A stage kept behind an argument nobody passes is a second way to build a feature that no gate exercises, so `from: "specify"` now throws with the list of stages this script has, exactly as any other unknown stage does.

Three things had to be found rather than decided. **Discovery.** With no specify stage there is nothing to return the feature directory, so preflight resolves it — `args.featureDir`, else `.specify/feature.json`'s `feature_directory`, which is where stock spec-kit 1.0.8's own `common.sh` reads the current feature from (its order is the `SPECIFY_FEATURE_DIRECTORY` environment variable, then that file; the branch name is a fallback only for the branch, not for the directory), else the branch name — and refuses when the directory does not resolve, when its `spec.md` is missing or empty, or when the checked-out branch is the base branch. **Discovery on every entry.** The same agent runs on a restart with its readiness checks stripped, because a `from: "plan"` restart used to leave `state.branch` null: the handoff table printed `(unknown)` and the finish agent was told to fast-forward from `HEAD@{1}`, both of which read as facts. `args.wall` is still required after preflight, since only the full check reads `CLAUDE.md` for it. **Where a spec finding goes.** The plan-review fixer and the analyze remediator return it under `specChanges`; the implement prompt makes it the third blocker of a forced task; the wall-repair prompts list a spec edit beside deleting a test. The needs-human reasons are thirteen across nineteen exits, five of them preflight's.

Rejected, and worth naming: leaving the fix agents free to edit `spec.md` as they always had, on the ground that nothing else changed about them. It loses to what the artifact is now for — the one thing in the feature directory no agent of the run wrote, and therefore the only oracle the run's own gates can be refuted against. The cost is that a spec defect stops the run instead of being absorbed, which is the point rather than the price.

Exercised by syntax check only, as with every change since 2026-09-20: the script is checked the way the sandbox evaluates it — the body inside an async function, where its top-level `await` and `return` are legal — since `node --check` on the file itself rejects both, and has since the script was written. No path added here was driven against the stub.

**2026-09-21, later: the run makes its own branch, syncs it, and stops trusting spec-kit's local pointer.** Ground: the owner's statement of how the team actually works — the domain expert may work only on the base branch (`dev` in `product-catalog`), writes `spec.md` there, and keeps editing it while a build runs — stated as his expectation of the skill: *started on the base branch, the run ends up on the feature branch, creating it when it does not exist*. **Nothing was executed; no Workflow run has started from this shape.**

Two facts were read out of `product-catalog` rather than assumed, and one of them corrects the entry above. `.specify/feature.json` is **git-ignored** — `.specify/.gitignore:6`, with spec-kit's own comment calling it *machine-local state, rewritten every time you switch features* — so on a fresh pull it names whatever feature this checkout last ran `/speckit-specify` on (it says `specs/004-product-gl-config` today). Resolving from it first, as the earlier order did, would build the previous feature from a file no commit has ever touched. And in stock spec-kit 1.0.8's `.specify/scripts/bash/common.sh`, `get_feature_paths()` resolves the feature from `SPECIFY_FEATURE_DIRECTORY`, then from that file, and **from nothing else**: `get_current_branch()` returns `$SPECIFY_FEATURE` or the empty string and never reads git. So there is no branch check to satisfy and no branch name to get wrong — `feature/005-x`, `005-x` and `dev` are equally acceptable to every speckit skill, because none of them is consulted — and, the other way about, resolving the feature correctly in this script does nothing for the stages unless that file or that variable is made to agree. A `/speckit-plan` run with a stale pointer writes into the previous feature's directory however right the script was.

So the order is `args.featureDir` → the branch name → on the base branch, the one directory under `specs/` with a non-empty `spec.md` and no `plan.md` → `feature.json` last and never overriding. The third is a rule rather than a heuristic: what this run builds is a feature that has been specified and not yet planned, and a repository holding two of those is genuinely ambiguous, so the exit lists every candidate and asks for `args.featureDir`. Rejected: taking the highest-numbered or newest directory, which turns an ambiguity a person can settle in one argument into a wrong build nothing downstream would notice. Then preflight writes `{"feature_directory": "<resolved>"}`, the single write it is allowed. Where a repository tracks that file instead, it is left alone and the stage prompts carry `SPECIFY_FEATURE_DIRECTORY`, which `common.sh` honours first — with the caveat stated rather than hidden: `get_feature_paths()` persists that variable into `feature.json` itself unless the caller passed `--no-persist`, a write this script cannot prevent.

The branch work replaces the base-branch refusal added earlier the same day. A full preflight started on the base branch checks out `args.branch`, else an existing `feature/<dir>` or `<dir>` (local or `origin/`), else a new `feature/<dir>` cut from where it stands — after the dirty-tree check, because every one of those moves the tree. Then the sync, on every entry whose branch is not the base: base already an ancestor, nothing; strictly behind, `merge --ff-only`; diverged, `merge --no-edit`; conflict, `merge --abort` and a `needs-human` naming the paths. Never a rebase — the branch may be pushed. The merge is conflict-free for the spec by construction, because no stage of the build writes `spec.md`; what it buys is that the author's later edits reach the run at all, and that the run does not discover a moved base at finish, where `merge --ff-only` into the base fails after the whole run has been paid for. A spec that changed under a run starting later than `plan` is its own exit, because `plan.md`, `tasks.md` and the code were derived from the old text; the restart is `from: "plan"`, which is what the handoff document already tells a reader to do after a spec change.

The handoff safety argument was re-examined and tightened rather than restated. `HANDOFF.md` is committed on whatever branch the run is standing on, so the guard is no longer *no feature directory* alone: `writeHandoff` also writes nothing while `state.onBaseBranch` is true. That covers every failure before preflight checked a branch out — the dirty tree among them — and the one legitimate case of a run that stays on the trunk, a later-stage start on a feature implemented there. The cost is named: such a run loses its committed handoff and keeps the report on its return value, which is the trade this file already accepts for a preflight refusal. *(Superseded 2026-09-25, below: a later-stage start on the trunk now gets its feature branch from preflight, so no run stays there past preflight.)*

Needs-human reasons are fifteen across twenty-one exits, seven of them preflight's. Exercised by syntax check only, the wrapped way this file describes; the seven starting states the change was designed against — base branch with no feature branch, with one ahead, one behind, one diverged cleanly, one diverged conflicting, a feature branch with a stale pointer, and a base branch with two unplanned specs — were traced by hand through the prompt and the script, not run.

**2026-09-22, a `needs-human` return is resolved by the session that launched the run, not relayed to a person.** Ground: the owner's decision, on one run. A run stopped at analyze on three findings whose only remedy was a change to the spec, and the operator resolved all three by taking the session's own recommendation unchanged — a person relaying an answer the session already had. **Nothing was executed: no session has applied a resolution under this rule, written a `RESOLUTIONS.md` or reported a stop as settled, and the tier stays *decided, not yet validated*.**

The rejected default is the one this skill shipped until today: relay every `needs-human` return to the person. It lost to a rule the skill was already built on. The owner's rule of 2026-09-18 — *a run never hands back work it could have done itself* — produced the forced convergence round, the bounded wall repairs and the reconcile behind a failed forced append, each of them a stop reclassified as work the loop could do. The return value was the one place the rule stopped applying, and only because nothing had said it reached past the script. The shape of the extension is the same as those three: an attempt, bounded at one per item, with a survivor escalating and saying so.

**What was *not* done is the point of the second attempt at this.** The first design gave the script a new stage that amended `spec.md` from a `specChanges` finding, with its own tier row, schema, prompt, audit file and argument. The owner rejected it as too specific — *"I wanted a general solution. If the session's main agent can make a good recommendation just follow it, don't ask for humans."* He is right about the shape, and the diagnosis is worth keeping: the specific design solved one exit out of twenty-one and would have had to be rebuilt, stage by stage, for every other exit a session could resolve. The general rule solves all of them and **moves no exit in `workflow.mjs` at all** — what the script stops on is byte-for-byte what it stopped on yesterday, and what changed is who reads the stop first. The lesson generalises past this repo: a rule written at the level of the *mechanism that produced an instance* ships a mechanism per instance; the same rule written at the level of *who is standing there when it happens* ships once.

Three things had to be decided. **Where the rule lives.** In `SKILL.md`, as directive text for the invoking session, because the actor is the session and not an agent the script dispatches; the script gained two sentences of the handoff document and nothing else. That change was made because `HANDOFF.md` opened *This run has stopped and is waiting on a person* and *The decision being asked of you*, which is the first thing the resolving session reads and was now false about its addressee. The restart arguments the session needs were already in that file — `from`, `wall`, `featureDir`, `branch` — so nothing else was missing and nothing else was added. **Why the session may edit `spec.md` when no agent of the run may**, which is the asymmetry this rule has to justify rather than assume: the ban inside the run is unchanged, and it rests on position, not authority. An agent inside the loop is one whose work the spec is the oracle for, and an artifact the run may rewrite cannot refute the run; the session is outside a stopped run, reading its whole report against the repository, and its edit is followed by a restart at the stage the handoff names, so every gate the edit touches runs again over the edited text. The restart is the safety, and an edit applied without one has none of it. **The test for acting, stated in the owner's own terms: high confidence in a good recommendation.** That is his wording and it replaced a *groundable* taxonomy the first draft of this section led with — an accurate description of what makes confidence high, mistaken for the test itself. The operational form is one sentence: it is the recommendation you would hand the person expecting them to take it unchanged; one answer, no hedge, and an alternative or a caveat that could change the choice means it is not high confidence. A low- or medium-confidence item still travels with its recommendation — the person gets an answer and what is missing from it, never a bare finding. One sentence about what confidence rests on survived the rejected design because it is the real guard: **the run's own output can corroborate and can never be the whole of it** — `plan.md`, `tasks.md`, `data-model.md`, the contracts and the migrations were written *from* the spec by the run that just stopped, so resolving a stop by making the spec agree with them is the run rewriting the document its own gates are judged against. On business values the rule is stated as the observed case actually behaved rather than as an absolute: a value nothing in the repository states or implies cannot be high confidence however obvious it looks, and one the spec already carries as its working value can. The first draft wrote *never invent a business value, however obvious it looks from the code*, which would have refused the very item the owner settled in three words — the spec's own working assumption already carried it, so reading it out was not inventing it. **The trace.** `<featureDir>/RESOLUTIONS.md`, append-only, one dated section per stop, each item carrying the recommendation applied, what the confidence rested on and the commit, or the fact that it went to a person and what was missing. A section appended to `HANDOFF.md` was the cheaper option and was rejected on one property: the script overwrites `HANDOFF.md` at the next exit by design, and the one-attempt-per-item test has to be answerable by a session reading a *fresh* handoff, without going through that file's git history to find out whether this item has been tried. Commit messages alone were rejected because a message carries the change and not the item it closed.

What the rule does not license is stated in the skill rather than left to inference, because this is the one place the owner's rule could be set against the definition of done and the rule has to lose: no gate weakened, no test skipped or deleted, no suppression, baseline or waiver row, no force flag, no commit on the base branch, and nothing that resolves somebody else's uncommitted tree or merge conflict — the last two for preflight's own reason, that acting on them destroys work which is not the run's. *Could have fixed it* does not include *could have hidden it*, and it does not include *could have thrown somebody's work away*.

Unmeasured, and worth naming: how often a session will in fact hold a recommendation with high confidence. *(Measured by the 2026-09-24 sweep, from the `RESOLUTIONS.md` files: sixteen stops, forty-one items applied by the session, eight taken to a person, and the person adopted the session's recommendation on all eight — see the last entry of this file.)* The evidence is three items of one stop, all of one kind, resolved by one operator. Whether the rule holds for a red wall at finish, a reconcile disagreement or a preflight refusal is the first real run's measurement, and the honest expectation is that the three forbidden classes above catch more of those than they did of the three observed. Exercised by nothing: this change is directive text plus two sentences of a rendered document, and there is no path through the script to drive.

**2026-09-22, the first read of the run journals, and what six days of real runs said about this file.** Until now every claim here was one of two kinds: a decision with its ground, or a path driven against a stub of the Workflow sandbox. Neither reads a run. Twenty-eight journals existed — `~/.claude/projects/<repo>/<session>/workflows/wf_*.json`, 2026-09-17..22, two service repositories, one machine, Claude Code CLI 2.1.278 — and none had been opened. The reader is `npm run runs` in the repository these skills distribute from (`https://github.com/dulguun0225/skills`, under `scripts/`); it is a report and not a gate, and every figure below moves when it is re-run, which is why the command matters more than the number.

**Four corrections, and the class they share.** (1) The forced convergence round of 2026-09-18 has run: seventeen `force-append` dispatches over four runs on 2026-09-20 — `wf_3dc51822-772`, `wf_fbb96088-993`, `wf_c6f73cab-e1f`, `wf_1ee836c9-35f` — fifteen of which became an implemented `Convergence (forced round n)` phase with the wall green, and 188 entries in `converge.forced` across the corpus. The two dispatches that produced no implemented phase are unexplained by anything in the journals and are owed. (2) The bounded wall repairs of the same day and the reconcile of 2026-09-19 have been taken by **no** run and no agent. (3) The 2026-09-21 shape, called unrun in the status line, has started **six** runs on 2026-09-21 and 2026-09-22, each of which exited `needs-human` at preflight, review-plan or analyze without reaching implement — so it is exercised at its front and unrun from `implement` onward, which is a different claim from either the one it replaced or the one it was mistaken for. (4) The cost this skill said it did not have: at least 28.6M subagent tokens over 250 of 276 agents that reported one, 17.5 hours, converge the dearest stage at thirty-nine agents and about 158,000 tokens each.

**The class.** The entries above at 2026-09-20 record the forced round running, in detail, with its run id — and the summary sentence two entries earlier and the status line in `SKILL.md` both went on saying no run had taken it. **The dated log grew and the summaries did not**, which is this set's worst recorded shape with the polarity reversed: usually `evidence.md` is corrected and the always-loaded `SKILL.md` keeps the stale sentence; here the always-loaded file and this file's own summary both lagged this file's own record. And the mechanism behind it is general enough to be worth naming once: the lapse rule decays a *confirmed* marker with no maintainer action, but **nothing ever promoted an *unrun* claim when a run took it**. A claim that something has not happened is as perishable as a claim that it has, and it decays in the flattering direction — it makes an untested mechanism and a working one read alike.

**What the journals cannot say, so nothing here may claim it.** No dollar cost — one undifferentiated token number per agent, no input/output/cache split. No skill names at all, so whether `money`, `primary-keys` or any other directive reached an implement agent is invisible from here. No distinction between a `converge-feature` run and this script started with `from: "converge"`. No wall attempts inside an implement agent and no second implement pass over unchecked ids. Agent prompt and result previews capped at 401 characters, so a finding the script did not lift into its return value is gone. Tokens null on 26 of 276 agents, which is why every total above is a floor and carries its coverage.

**Two findings this file does not resolve.** One feature — `reference-data` `001-company-registry` — took eight runs and 5.62M tokens across 2026-09-21..22 and never reached implement, three of its analyze exits carrying the same reason: a finding whose only remedy is a change to `spec.md`. That is the loop at a fixed point it cannot leave, and it is the baseline for the resolution rule of 2026-09-22 rather than evidence against it, since the rule is that day's. And `RESOLUTIONS.md` has never been written in either service repository across eight recorded stops *(dated by the 2026-09-24 sweep: written for all sixteen stops of 2026-09-22..24, in five feature directories)*; the two resolutions that were recorded were written by overwriting `HANDOFF.md`, which is the exact property this file's entry above rejected that file for. One sweep is not evidence about a rule one day old, so nothing is changed on it today — but the next sweep compares its rate against `convergence-rationale.md`, which nobody instructed and which was maintained.

**2026-09-23, the roster is Opus only.** Ground: the owner's decision — every subagent runs on Opus 5.5, at low, medium or high effort; no Haiku, Sonnet or Fable. The owner's global agent roster was switched the same day, so the reason the 2026-09-18 entry gave for this script's roster — a stage here priced like the same work anywhere else — still holds, on the new roster. `ROSTER` in `workflow.mjs` is now `opus` at `low`, `medium` and `high`, and `tier()` refuses every other model, and `xhigh` and `max`, from the table or from `args.tiers`, before the first agent starts; exercised against a stub the same day, where `sonnet low`, `fable low`, `opus xhigh`, a full model id and an override on an unknown row each threw and the default table passed. The mapping, stage by stage: preflight, phases, finish and handoff, Sonnet low → Opus low; plan and review-plan, Fable low → Opus high, since they had been on the roster's highest tier and Opus high is now that; fix-plan, tasks, analyze, remediate, remediate-critical, implement, converge, force-append and reconcile-tasks, Opus medium, unchanged. The model is the alias `opus`, which resolves to Opus 5.5 on 2026-09-23. It is not a pinned id like `claude-opus-5-5`, because the Workflow authoring reference, read that day, names no accepted values for `agent()`'s `model`, and the Agent tool's `model` is an enum of aliases. So a later Opus release moves every row with no edit here, and that is a known exposure, not an oversight.

**Unmeasured, and what it makes stale.** No run has taken these tiers. *(Dated by the 2026-09-24 sweep: twelve runs have, and the entry below prices them.)* Every per-agent token and wall-clock figure in the 2026-09-22 entry, in `SKILL.md`, and in the run ledger kept by the repository these skills distribute from (`https://github.com/dulguun0225/skills`, beside `npm run runs`) was taken on the old table, Sonnet and Fable rows included, and none of them prices this one. The row to watch is plan and review-plan at Opus high. The 2026-09-17 record is two runs at Fable high and xhigh that spent 1.19M and 0.91M subagent tokens mostly in artifact reviews, so high effort on exactly these stages is the recorded way this script got expensive — on another model line, which makes it a warning and not a prediction. The next sweep of the journals is the first measurement of this table, and until it runs, this paragraph is the claim to re-read against it.

**2026-09-24, the second read of the run journals: two defects the stops were made of, and one prompt the owner's rule already forbade.** Ground: forty-nine journals, 2026-09-17..24, three service repositories, one machine, Claude Code CLI 2.1.278 to 2.1.281, read with `npm run runs` in the repository these skills distribute from, together with every stop's `HANDOFF.md` and the commits after it in the service repository's git history. The owner's aim for the sweep was stated the same day: no stop after the spec is signed off that an earlier stage could have prevented, so every stop was traced to the earliest stage whose inputs already held what was needed. **Three changes to `workflow.mjs`, each exercised against a stub of the Workflow sandbox and none by a run.**

(1) **Preflight found the definition of done and the script dropped it.** `state.wall = p.wall || state.wall` was deleted in `c4b9a16` (2026-09-21) with the block around it, so from then every full preflight without `args.wall` returned `wall: "node backend/scripts/wall.mjs"` in its own result — read from the project's `CLAUDE.md`, as the prompt says — and the script stopped on *no definition-of-done command* anyway. Six stops on six features, the first run of every feature since that commit; each was resolved by passing the value the agent had already found. The line is restored, with `args.wall` winning where given, since every argument overrides what preflight finds. The stub reproduces the stop on the committed script and a clean pass on the corrected one.

(2) **`from: "review-plan"` ran no review.** The review loop sat inside the plan stage's `runs('plan')` guard, so a start at `review-plan` skipped both and went to tasks, while every `needs-human` exit at that stage names `from: "review-plan"` as its restart. Seven review-plan stops were restarted that way, 2026-09-21..23; on each the session had applied the fourth review's findings by hand, or on the first of them nothing at all, and no reviewer read the result before tasks. That is the one property the resolution rule leans on — *the restart is the safety* — failing silently at the stage the loop stops at most. The guard is split: `from: "review-plan"` reviews the plan on the branch without regenerating it, and `until: "plan"` now stops before the review. It also means the round-cap counterfactual recorded in the 2026-09-18 audit above looked taken and was not.

(3) **A task that waits on a person.** On `reference-data` 003 an analysis remediation added T104 — close only once the spec's owner has run a `/speckit-clarify` session answering the plan's readings — and gated two closing tasks on it. The run built eight phases wall-green and stopped at implement on the one task no agent could close; the owner then confirmed every reading unchanged. The plan had already decided those readings under this skill's own rule, so the task was a human gate written back into an unattended run. The tasks and remediate prompts now carry `NO_TASK_WAITS_ON_A_PERSON`, and a question only the author can answer goes to `specChanges`, where the run stops at the stage that found it rather than after implement. *(Same day, on adversarial review of that change: the tasks stage's `specChanges` was read by nothing — its schema told the agent the field stops the run, and the script dropped it — so a question this ban routed there left the run without a trace. The tasks stage now stops on it as review-plan and analyze do; exercised against the stub, not by a run.)*

**What the sweep measured, beside the defects.** The Opus-only table has run twelve times: review-plan at Opus high averaged about 229,000 tokens per agent, plan about 160,000, against about 241,000 and 175,000 for the same rows at Fable low over the sweep's nine older runs — high effort there did not repeat the Fable-high spend, at *convention*, since nothing was controlled. The resolution rule has run: every one of the sixteen stops of 2026-09-22..24 has a `RESOLUTIONS.md` entry, forty-one items were applied by the session and eight taken to a person, and the person took the session's recommendation on all eight. That is the first number for this file's *how often a session will hold a recommendation with high confidence*, and read against the owner's aim it says the bar is set conservatively rather than loosely; whether to lower it is his, not a change made here. The bounded wall repair has run once and worked; the reconcile still never.

**What was decided and not applied**, each on the backlog of the repository these skills distribute from with its stop ids as ground: the review and analyze loops' stop rule — of the eleven cap stops on record, two needed an owner's design decision (`wf_c65ab41b-8a6`, a lock protocol the review had refuted three rounds running, replaced by a single write lock; `wf_e5ab8e1a-c53`, a declared gap the owner chose to close) and the rest were resolved by applying the last round's findings unchanged, or once by applying nothing — which is a decision for the owner, because the 2026-09-18 audit above put *apply and move on* in his hands; the restart after the session's own spec edit, which this skill sets at `plan` although it rejects re-running plan everywhere else, and which regenerated the plan into a fresh review-plan cap on five of eight restarts; a spec-readiness pass the author runs before sign-off, for the eight stops whose cause was already in the spec's text — published 2026-09-24 as the `spec-readiness` skill, run by the author and not by this script; and the handoff's journal line, which fails to name its own run's journal on twenty-five of twenty-seven handoffs — twenty-three name an earlier run's, two found none — because the step takes the newest journal on disk and the running one is, by inference, not yet there. Nothing in this entry was executed by a run. *(All but the spec-readiness pass were decided by the owner and applied later the same day — see the last entry of this file.)*

**2026-09-24, later the same day: the ban reaches the two stages that still write tasks, and a later start checks its inputs.** Ground: two changes the owner approved the same day, after the adversarial review of the entry above. **Both exercised against a stub of the Workflow sandbox and neither by a run.**

(1) **Converge and the forced append carry `NO_TASK_WAITS_ON_A_PERSON`.** They are the other two stages that write tasks, and neither had the ban or a route for the question it sends away: the converge schema had no `specChanges`, and the forced append is told to write exactly one task per finding. Both schemas now carry the same `specChanges` field as the fix and remediate rows, written once as `SPEC_CHANGES_FIELD`, and the script reads it at every converge-stage return — each assessment round before its outcome, the assess-only round, the forced append and its retry — and stops the run with a handoff. The forced append, given a finding only the author can answer, appends nothing and returns `appended: false` with the question; that is read before `appended`, so it is not taken for a failed write and sent to reconcile. Where an assessment appended a phase beside a question, the phase is left unimplemented, since the question's restart is at `plan`. The constant's closing clause — *where you return `specChanges`, it goes there, and otherwise it is left out* — is now *it goes in `specChanges`, which stops the run*: every stage that carries it returns the field, and "left out" was a silent drop, the defect class `211809f` fixed at tasks. The stub showed each of the five new paths ending `needs-human` at `converge` with a handoff, and the same cases ending otherwise on the committed script — `done` for a question on an assessment or on the assess-only round.

(2) **A start after `plan` checks the feature artifacts it reads.** The list is derived per stage, not taken from an example: spec-kit's `check-prerequisites.sh` refuses a missing `plan.md` in every mode and a missing `tasks.md` under `--require-tasks`, which `speckit-analyze`, `speckit-implement` and `speckit-converge` pass and `speckit-tasks` does not (read 2026-09-24 in a service repository's installed skills); review-plan's prompt refutes `plan.md`; the phase reader and finish read `tasks.md`. What a stage earlier in the same run writes is dropped, so `until` narrows it. The preflight agent tests each with `test -s` and returns failures in `missingInputs`; the script counts only the files it asked about and stops before the `ok` test, so the specific exit wins where the agent also listed a problem. The restart is the earliest stage that writes a missing file, or `plan` where the sync also changed the spec, and it is carried as `detail.restartFrom`, which the handoff's restart paragraph now follows instead of naming `preflight`. A full run and a `from: "plan"` start check nothing new; `converge-feature` on an implemented feature, on a feature branch or on the trunk, ran to `done` on the stub.

**Not verified.** Whether an agent routes a question to `specChanges` rather than writing the task anyway is read off `tasks.md` after a real run. A forced append that disobeys and writes part of a phase before returning `specChanges` leaves `tasks.md` dirty when the run stops; the handoff commits by pathspec, but the next preflight refuses the dirty tree. The *high-confidence* test is unchanged: the question still goes to the launching session first.

**2026-09-24, evening: the owner's decisions on the review and analyze stop rule, the restart after a spec edit, a deferral in the spec, and the journal line.** Ground: the owner's decisions of the day, taken on the per-stop record of sweep 2 in the run ledger of the repository these skills distribute from, each with its stop ids there. **Exercised against a stub of the Workflow sandbox and by no run.**

(1) **A survivor stops the review and analyze loops; the cap does not.** Of the eleven cap stops on record, nine were resolved by applying the last round's findings unchanged or, once, by applying none — `wf_543886c7-762`, `wf_9cb9df52-800`, `wf_327e7ce1-c3a`, `wf_e6f0fb71-2ab`, `wf_9ecfdaff-a93`, `wf_510277de-2c6`, `wf_11cbb069-ff1`, `wf_85172f44-a39`, `wf_c3abfa9f-b08` — and the rounds between showed no fixed point, a fix applying four to fifteen findings and the next review finding new ones. So the cap now does what converge's does: `maxReviewRounds` ordinary fix rounds, then the review after the last of them is applied too, and one final review reads that fix. The run stops only on a survivor — a serious finding restating one an earlier fix round was handed — which the script decides from `repeatOf` against a numbered list the reader is given, as converge has done since 2026-09-20, or from exact normalised identity. The final review's non-survivor findings are reported open and not applied, because applying them would put an unreviewed fix in front of tasks; the minors pass on an approve is the one unreviewed fix left, and it predates this. The two cap stops the owner had to decide were read back from their handoffs to check the design stops them. `wf_c65ab41b-8a6` (`77daa3b` in the service repository): the round-4 finding refutes R5's lock protocol again — its no-deadlock claim, false in the step round 3's fix added — where round 2's finding had refuted the same protocol's race proof. That is a restatement under the rule's own wording only if the reader takes *the protocol is correct* as the one property both findings name (*the fix replaced the mechanism and the replacement fails the same requirement in a new way*); read so, the run stops at the first such round rather than at the cap, and read as two properties it stops at none and goes on to tasks with the protocol open — the one outcome of this change that would have needed the owner and not reached him. `wf_e5ab8e1a-c53` (`8b8893a`): the round-4 HIGH is decision D-a narrowing `001/FR-024`; D-a was written by the round-1 remediation (`c7e9c53`) in answer to a finding against the same requirement, so it restates that finding (*the fix declared the gap or narrowed the requirement instead of closing it*), and the run stops at analysis 4 before the cap fix. Both are modelled in the stub with the labels a reviewer following the prompt would give; **whether a real reviewer gives them is the unmeasured half**, and journal previews capped at 401 characters could not show the round-1 severity or wording of either.

(2) **The restart after a spec edit is `from: "review-plan"`.** This skill rejected re-running plan everywhere except here. On eight restarts at `plan` after a spec edit, the regenerated plan hit a fresh review-plan cap five times — `wf_c65ab41b-8a6`, `wf_9ecfdaff-a93`, `wf_11cbb069-ff1`, `wf_85172f44-a39`, `wf_c3abfa9f-b08` — which is what held `reference-data` 001 at its fixed point. Every exit whose remedy is a spec change now returns `restartFrom: "review-plan"`, and the handoff's restart paragraph and reason follow it; the preflight exit for a spec the sync moved now stops only a start later than `review-plan`, and its handoff no longer says `from: "preflight"` while its reason says `plan` (the wording defect sweep 2 found on the same exit). The review that a start at `review-plan` runs is told the spec may have moved since the plan was written and that a disagreement with its current text is a blocking finding fixed in place. Not decided, and stated in the skill: a spec edit after converge still regenerates `tasks.md` at the tasks stage.

(3) **A deferral the owner wrote into `spec.md` wins over forcing.** `wf_e4af9888-80a` forced and built `003/FR-034`, which the owner's clarification in the spec had deferred; he kept it, and decided the rule the other way for next time. The assessment now returns such a finding with `deferredBy` quoting the spec text; the script leaves it out of the floor test, the forced append and the survivor test, and reports it under `converge.deferred`. Only `spec.md` counts — a deferral in any artifact the run writes is the run's own output, which the resolution rule already refuses as the whole ground for anything. The script cannot check the quote, which is the stated gap.

(4) **The handoff names no journal path.** The Workflow API gives a script no handle on its own run id (its authoring reference, read 2026-09-24), so the lookup was removed rather than repaired: the document says the run id is on the launching session's tool result and gives the journal's location pattern, the session records the run id in `RESOLUTIONS.md`, and the handoff agent substitutes the date alone.

Stub: 107 checks pass on the edited script — every from/until pair still `done`, the earlier checks of the day, the review and analyze survivor and cap paths, both owner-needed stops, the four spec-change exits and the preflight one, the deferral paths and the handoff prompt — and 35 of the 41 new checks fail on the committed script, the other six being controls for behaviour meant not to change. The needs-human reasons stay sixteen across twenty-seven exits: the two cap exits became the two survivor exits.

**2026-09-24, late: an adversarial review of the evening entry, before any service repository runs it.** Ground: the full agent transcripts of `wf_c65ab41b-8a6` and `wf_e5ab8e1a-c53`, which sit beside their journals under `subagents/workflows/<run id>/` and carry every prompt and return whole, where the journal previews the entry above read stop at 401 characters. **Exercised against a stub of the Workflow sandbox and by no run.**

(1) **The lock-protocol case rests on the rule's plain reading, not only on its widest one.** Round 2's review refuted R5's race proof; round 3's refuted R5's no-deadlock property — the fixpoint re-lock that round 2's fix added waits for a row while holding one, `40P01`; round 4's refuted the same no-deadlock property at the same place again, after round 3's fix restricted the re-lock (*"the round-3 fix … broke the no-deadlock proof it rests on"*). Round 4 therefore restates the round-3 entry — the same property, the same place, still unmet — whatever a reviewer makes of the round-2-to-round-3 link; its second major finding, a test seam that does not drive the production loop, restates round 3's missing-seam finding under *the replacement fails the same requirement in a new way*. So under the shipped rule the run stops at round 4, before the cap fix, if the reviewer labels either; the entry above said it held only on the wider reading. Both readings are now modelled on the real four rounds, and the one where no round is ever labelled — which reaches tasks at `round-cap` — is modelled too. The analyze case reads as before: `A1`, the round-1 finding the remediation answered with decision D-a, came back as a `MEDIUM` in round 3 that the remediator skipped as *already a named gap*, and as the `HIGH` `G1` in round 4.

(2) **The final review's open findings reached nothing.** They were on the return value alone, of a run that goes on to implement, and no later prompt carried them; the skill's sentence that analyze reads the plan and tasks again implied a coverage it does not have, since `/speckit-analyze` checks artifacts against each other and a deadlock is not a question it asks. The first analysis is now handed the final review's open blocking and major findings, returns each that still holds — a blocking one no lower than `HIGH`, so it is remediated — and the next analysis reads the fix, where the survivor test applies once more. Nothing stops on them that did not before; the owner's rule that the final review stops only on a survivor is unchanged. **Owed to the owner**: whether a blocking finding at the final review should stop the run outright instead, which this does not decide.

(3) **A deferral quotation is checked before it is honoured.** One parse-only agent (`checkDeferrals`, Opus low) searches `spec.md` for each new quotation, whitespace and Markdown emphasis aside; one it does not find, or does not answer for, is dropped, and the finding is forced like any other and listed under `converge.deferralsRefused`. Ground: the evening entry's stated gap — *a quote the assessment invents is taken as given* — was the one place where the assessment's word alone decided that work is not done, and a line quoted from `plan.md` would have exempted exactly what the owner's rule says only `spec.md` can. What it does not check is whether a quotation it finds defers the item it is attached to.

(4) **A survivor carries what its round's fix agent declined.** The fixer's and remediator's `skipped` lists were discarded, so a handoff could not tell *applied and did not take* from *declined, with this reason* — and on `wf_e5ab8e1a-c53` the second is what happened in round 3. The whole list of the round that handed the survivor is attached as `skippedByThatFix`, since a free-text entry cannot be mapped to its finding.

**Confirmed and not changed: the restart at `review-plan` over an implemented feature.** *(Decided 2026-09-25, below: the tasks stage now updates a ticked `tasks.md` in place.)* A start there runs the tasks stage, which regenerates `tasks.md` with every task unchecked and is told nothing about implemented work, and implement then runs every phase again over code that exists; the stub shows it. It is the cost the skill already states, and a guard — the tasks stage updating a ticked `tasks.md` in place, or refusing to regenerate over one — is a design choice spec-kit has no command for, so it is owed to the owner in the backlog of the repository these skills distribute from rather than decided here. Also read and left alone: a `repeatOf` naming a handed minor makes a survivor, as the owner's wording says (*one an earlier fix round was handed*); in analyze that includes a `MEDIUM` or `LOW` the remediator was permitted to skip, which is how the `FR-024` case stops, and is a false positive wherever analyze grading drifts upward on an item nobody tried.

Stub: every from/until pair still `done`; 137 checks pass on the edited script, 30 of them new, and 10 of the new ones fail on the committed script; the six earlier deferral checks pass only with a stub that answers the new check agent, which the earlier stub had no row for, the rest being the real-sequence models, controls and edges — `repeatOf` 0, negative, out of range, into a minor, across three rounds, against a deduplicated list, and same location with a different problem. The stub now shadows `Date`, `process` and `require` as the sandbox does, and checks every stubbed return against the schema the script passed, with a negative control that must trip it. The needs-human reasons stay sixteen across twenty-seven exits: none was added.

**2026-09-25: the owner's decisions on the three guards the 2026-09-24 entries left open.** Ground: the owner's decisions of the day, on the backlog rows those entries opened in the repository these skills distribute from. **Exercised against a stub of the Workflow sandbox and by no run.**

(1) **A blocking finding at the plan review's final round keeps the hand-over and does not stop the run.** Its open blocking and major findings still go to the first analysis, a blocking one graded no lower than `HIGH`, and the survivor test still applies one round later. The rejected alternative is a stop on any blocking finding at the final review, which is the cap stop back for exactly the findings nine of the eleven cap stops on record were resolved by applying unchanged. The rest named on 2026-09-24 still stands: the `HIGH` floor is a sentence in the analysis prompt and not a check in the script, so an analysis that grades a carried blocking finding lower lets it reach implement unremediated. No script change.

(2) **A restart over an implemented feature updates `tasks.md` in place.** Preflight now returns `checkedTasks` — the ids its `grep` finds ticked, read after its checkout and its merge so the file is the one the run builds from — whenever the tasks stage will run, and returns it empty otherwise; a non-empty list puts that stage in update mode. The choice of preflight over a separate reader: it already reports repository facts on every entry, it runs before any stage writes, and the extension is one field and one prompt line. In update mode the stage does not run `/speckit-tasks`' generation, which writes the file whole: ticked tasks stay ticked, forced convergence phases included; a ticked task whose requirement the change altered is unticked with `(reopened: <what changed>)` in its line; a task no longer needed becomes `- ~~T015 …~~ (removed: <reason>)` *(changed later the same day, below: one line, the id only, naming no `FR` or `SC` id)*; new work gets new unticked tasks with ids after the maximum. **The removed representation is a decision, because spec-kit has none**: its checklist format has `- [ ]` and `- [x]`, speckit-implement counts only those two patterns, the service repositories' traceability gate treats any `- [ ]` as a feature still in flight (`OPEN_TASK` in `check-traceability.mjs`, read 2026-09-25 in `reference-data`), and `/speckit-converge`'s append contract takes the next id from the maximum in the file (its installed `SKILL.md`, Step 7, read the same day). A box of either kind is false for a task nobody will do, and deleting the line lets a later append reuse its id; so the box goes and the line and id stay, and the phase reader, analyze, converge and finish prompts carry one sentence saying such a line is not a task. **The update is certified**: one parse-only agent on the `phases` row reads the file afterwards and reports each id preflight listed as checked, reopened, removed, unchecked or missing; one that is not ticked, or reopened or removed with a reason, stops the run at tasks before analyze. The ground is the `phases` reader's own: the agent that edits a file is not the one that says what it holds, and a lost tick is either work implement redoes on nobody's decision or a record of done work that is gone. A first run is unchanged: with nothing ticked the stage is the generation it was, and the stub compares its prompt byte for byte with `396891a`'s. The rejected default is that generation on every run, confirmed on the stub on 2026-09-24: every phase implemented again over existing code, and the forced convergence phases gone from the file.

(3) **A run never commits on the base branch; a later start there makes the feature branch first.** Until this day a later-stage start on a feature implemented on the trunk — `converge-feature` closing one out — ran where it stood, with no branch and no sync, and its converge, implement and fix agents committed on the trunk, while `SKILL.md` said the run never does. Preflight's later-stage prompt now checks the tree is clean when it starts on the base branch — `git checkout -b` carries uncommitted changes onto the new branch, where the run would commit them, so the order is the full run's — and then runs `git checkout -B <target>` at the base branch's HEAD, under the name a full run derives (`args.branch`, an existing `feature/<dir>` or `<dir>`, else `feature/<dir>`). An existing branch of that name is reused only where it and its remote copy are ancestors of that HEAD, so moving it forward loses nothing; one holding commits the base lacks is a preflight problem, since which of the two places holds the feature is not the run's to choose. The script also checks the agent's report: a preflight that says it is still on the base branch while any later stage is to run stops the run there, with no later agent dispatched and no handoff committed. The return value carries `createdBranch`, and the handoff's table says when the run made the branch. After this the only exits taken on the base branch are preflight's own, before it moves the run: a dirty tree, a feature it cannot resolve, a branch in two places, and the still-on-base check. The rejected default was to stop and ask a person, for a mechanical act a full preflight already performs.

Stub: every from/until pair still `done`; 182 checks pass on the edited script, 45 of them new, and 38 of the new ones fail on `396891a`, the other seven being controls for behaviour meant not to change — the first-run tasks prompt byte for byte, generation over a file with nothing ticked, the full preflight's own branch step, a preflight-only run on the base branch, the dirty-tree exit, the implement stage over the reader's phases, and the update-mode `specChanges` exit. One earlier check changed with the behaviour: the trunk `converge-feature` case, whose fixture is now an agent that made the branch, while the old fixture — still on the base branch — is the new guard case. The needs-human reasons are now eighteen across twenty-nine exits: the still-on-base check and the certification exit were added.

**2026-09-25, later: adversarial review of that change, before the service repositories run it unattended.** Ground: the change's diff and the whole script; the three service repositories' `backend/scripts/check-traceability.mjs` (identical in `reference-data`, `product-catalog` and `customer-party-adapter`), their installed `speckit-*` skills, and their eleven `tasks.md` files, read with `git show`; the gate itself run on copies against fixture trees. **Stub and gate fixtures only; no run.**

- **The removed line as decided broke the wall.** The gate reads every `FR`/`SC` token anywhere in `tasks.md`, not only on task lines: every qualified token must resolve against `spec.md` (its check b), every bare one inside the feature directory must be defined there (b2), and any token counts as the requirement being "named by some task" (g). Run against a copy: `- ~~T002 build 001/FR-003~~ (removed: FR-003 was deleted from the spec)` fails the gate on both tokens — and a deleted requirement is the commonest reason to remove a task — while `- ~~T002 build 001/FR-002~~ (removed: …)` passes a requirement no live task names, which `- ~~T002~~ (removed: …)` fails as it should. The real files are mostly multi-line tasks whose continuation lines cite ids (`003-constant-registry`: 1,484 lines for 108 tasks), and the decided form kept those lines. **Changed**: the removed task becomes one line, `- ~~T015~~ (removed: <what it was for and why it is unneeded>)`, naming no id; its text is in the update commit's parent. Rejected: moving removed tasks to a trailing section the readers skip — the gate reads the whole file, so a section changes nothing for it, and it reorders the file. Also added: an id the edited spec no longer defines leaves every line of `tasks.md` (the one change a kept ticked line may take) and a new task takes it out of the code, tests and waiver rows, which the update prompt did not ask for and the gate refuses otherwise.
- **Readers that did not know a removed line.** The implement prompt names its phase as a range, `tasks T012–T020`, which spans a removed id, and carried no removed-task rule; the analysis remediation and the forced append take the next id from the maximum and were not told a struck id counts. **Changed**: all three carry it. `speckit-implement`'s own checkbox counting is for checklist files, not `tasks.md`, and `speckit-converge` Step 7 scans every id, so neither needs more.
- **The certification checked the ticked half of the rule only.** An open task deleted outright (its id reusable by the next append, its requirement unplanned) or ticked by the update (work nobody did, which implement then skips) passed it. **Changed**: preflight returns `openTasks` beside `checkedTasks` from the same `grep`, the certifier is handed both, and an open task must come back open or removed with a reason; the update prompt now says it ticks nothing. Renumbering shows as a missing id and was already caught for ticked ids. Not caught, and not catchable by a parse-only reader with no earlier text: a reworded task, and a phase heading dropped while its tasks stay — the second changes no task's state, so implement is unaffected; both are read off the update commit's diff.
- **The task list could be skipped.** The `grep` sat inside step 6 of the preflight prompt, and step 2 says that when no base branch resolves "steps 5 and 6 then do nothing" — so with no base found, an implemented feature's ticked tasks were never listed and the tasks stage regenerated the file over them. **Changed**: step 2 says what does nothing, and the list is read whether or not a merge ran. On every entry path the read is after the checkout and the merge: a full run on the base branch, a later start on it (branch made at the same commit, sync `none`), a start on the feature branch, and a restart after the base was merged in; `converge-feature` runs no tasks stage and is told to return both lists empty.
- **Branch safety.** The ancestry check before `git checkout -B` is the agent's alone; the script cannot run git and cannot verify it, and a skipped check moves a local branch holding work the base lacks back to the base HEAD, leaving that work reachable only from the reflog. **Changed**: the prompt makes the checks and the checkout one `&&` command, and a branch that exists nowhere is made with `git checkout -b`, which git refuses over an existing one. A remote-only target, checked with `origin/<target>`, is made locally at HEAD without tracking, and finish's `git push -u origin HEAD` then fast-forwards it; nothing is fetched, so a remote that moved since the last fetch fails that push, reported and never forced. **Detached HEAD**, a gap on every entry and not only this change's: `git rev-parse --abbrev-ref HEAD` prints `HEAD`, which is not the base branch, so every stage committed on no branch and finish's push refused. **Changed**: step 1 makes it a problem, the script's guard stops a run that preflight reports on `HEAD`, and no handoff is committed there. The guard also now takes a branch named the base branch as the base branch, rather than resting on the agent's `onBaseBranch` alone.
- **Forced phases across runs.** Since the change keeps an earlier run's `Convergence (forced round n)` phases through a restart at `review-plan` (a restart at `converge` always kept them), the reconcile path's certification — which counted every phase whose title contained `forced round <n>` — read an earlier run's round 1 as this run's, and stopped on a false disagreement; it also found round 1 in `forced round 12`. **Changed**: the round number is matched whole and a phase with no open task is left out. A partly ticked earlier phase of the same round would still collide; none is on record. The reconcile path has still never run.
- **Checked and left alone.** The stop count — eighteen reasons across twenty-nine exits — still matches the script: the detached HEAD joins the not-ready and still-on-base exits rather than adding one. A later start on the base branch with a missing input makes the branch before it stops, so that exit's handoff is committed and pushed on a new branch that holds only the handoff; the skill now says so. Of the fifty-four `build-feature` journals on this machine, forty-two passed `baseBranch: "dev"` and twelve passed none; the service repositories have no `origin/HEAD` and a local `main`, so a run that omits it takes `main` as the base, and one started on `dev` then reads `dev` as a feature branch and merges `main` into it. The skill already says to pass it; nothing here changes that, and it is named for the owner. *(Decided later the same day, below: the base branch is discovered, and a clone like these stops at preflight.)*

Stub: 224 checks pass on the edited script, every from/until pair still `done`; 42 are new, of which 29 fail on `e2699a8` and the other 13 are controls or unchanged behaviour. Ten earlier checks changed with the text they test — the removed-line form, the task `grep` and its empty-list instruction, and the later-start checkout command — and every other earlier check passes on both revisions.

**2026-09-25, later still: the base branch is discovered, and the input artifacts are checked before a branch is made.** Owner's decisions, the same day. Ground: the whole script; the three service repositories read with `git -C … symbolic-ref refs/remotes/origin/HEAD`, `git -C … branch`, `git -C … branch -r` and `git -C … show-ref`, read-only; `git remote set-head origin <branch>` run in a scratch repository whose remote URL does not resolve. **Stub only; no run.**

- **The base branch.** Precedence: `args.baseBranch`; else `git symbolic-ref --short refs/remotes/origin/HEAD` with `origin/` stripped; else the one local branch among `main`, `master`, `develop` and `dev` where exactly one exists; else unresolved, which is `needs-human` at preflight before preflight's own agent runs — nothing checked out, merged, written or committed, and no handoff, since the run does not yet know whether it stands on the trunk. `git remote show origin` is excluded because it contacts the remote, and so is `git ls-remote`; the fix the stop names, `git remote set-head origin <branch>` with an explicit branch, wrote `refs/remotes/origin/HEAD` in the scratch repository with its remote unreachable, and refused a branch with no remote-tracking ref. The facts come from one read-only Opus-low agent dispatched only when `baseBranch` is absent, and the script resolves from them (`resolveBase()`), because the agent that also checks out and merges would otherwise apply the rule and act on it in the same turn, and a misapplied rule was the defect; preflight's agent is handed the base and its echo of it is no longer read, so the still-on-base guard compares the branch against the resolved base. The return value carries `baseBranch` and `baseBranchSource` on every return, and the handoff's table names the source. **What discovery returns in the service repositories, 2026-09-25**: `reference-data`, `product-catalog` and `customer-party-adapter` each have no `origin/HEAD` (`symbolic-ref` fails), the local branches `dev` and `main`, both on the remote too, and `dev` checked out; `dev` is the base branch their authors work on, by the owner's correction when the unattended build first met the service repositories. So the owner's example fallback — the one local branch among `main` and `master` — answers `main` in all three, which is the defect being replaced; the fallback therefore also counts `develop` and `dev`, whose presence makes `main` ambiguous, and in all three repositories discovery resolves nothing and the run stops until `baseBranch: "dev"` is passed or `git remote set-head origin dev` is run once per clone. Rejected: `main` as a silent default, and refusing every run without `baseBranch`. *(Superseded as the fix later the same day, below: the project's `CLAUDE.md` states the base branch in one line.)*
- **Missing inputs before a branch.** The later-stage step 5 now decides the branch the run would work on before any checkout: target absent, or every ancestry check passing — the base branch's HEAD, where the artifacts are read and a missing one stops with no branch made; target ahead of the base and the base holding any artifact — the work-in-two-places refusal, unchanged; target ahead of the base and the base holding none — the target, read with `git cat-file -s` on the local branch or `origin/<target>`, stopped on a missing artifact with nothing checked out, and otherwise checked out and the base merged in as a full run does. Until this change that last case was refused as work in two places, though the base held none of the feature's work. A later start that reads no artifact (`from: "plan"`) keeps the refusal. On every start that ends on a feature branch the artifacts are read again after the sync, which is the tree the stages read; until now they were read before the sync, so a merge that added or removed one went unseen. The script cannot see whether the agent stopped before `git checkout -b`: where preflight reports `createdBranch` beside a missing artifact, no handoff is committed on that branch, the return says so, and the branch holds no commit of the run.
- **Counts.** One reason and one exit added — the unresolved base branch: nineteen reasons across thirty exits, ten of them preflight's. The missing-input exit moved earlier and is not new.

Stub: 266 checks pass on the edited script, every from/until pair still `done` — with the base discovered, passed, and taken by fallback; 42 are new, of which 33 fail on `ab06221` and the other nine are three controls that show `ab06221`'s defect, five controls for behaviour meant not to change, and one reading of `SKILL.md` alone. Two earlier checks changed with the behaviour: the preflight prompt's no-base wording, now absent, and the trunk later start with a missing `tasks.md`, whose fixture is now an agent that stops before making the branch. The harness counts the new discovery agent apart from the agents from preflight on, so every other earlier check reads as it did.

**2026-09-25, last: the project's `CLAUDE.md` states the base branch, and the script reads it.** Owner's decision, the same day. Ground: the discovery entry above left every run in the three service repositories stopped at preflight without `baseBranch`, since each has no `origin/HEAD` and both `dev` and `main`. Rejected by the owner: `git remote set-head` per clone, lost on every fresh clone, and `baseBranch` on every run, a person step on every run. **Stub and scratch repositories only; no run.**

- **Precedence and format.** `args.baseBranch`; then one line in the repository root's `CLAUDE.md`, `` Base branch: `<branch>` ``, unindented, alone and once, parsed by the script against `BASE_LINE` in `workflow.mjs`; then `origin/HEAD`; then the single local trunk name; else the stop. `baseBranchSource` gains `claude-md`. The format copies how the definition of done is stated — a line in the same file — and is exact rather than descriptive, so that the agent returns grep output and the script, not the agent, decides what the line says.
- **Which `CLAUDE.md`.** The repository root's, as committed at `HEAD` of the branch the run starts on: `git show HEAD:CLAUDE.md`. The base is not yet known when it is read, so no other branch can be chosen; a feature branch carries the line of the base it was cut from or last merged, and the line names the trunk, which does not move with the feature. A feature branch cut before the line was committed carries none and takes the next source down; the unresolved stop now names that case and its fixes — start from the base branch, merge the base in, or pass `baseBranch`. Not a backend's `CLAUDE.md`, which in a vendored project is `java-backend-template`'s own and states the template's trunk; not the working tree, since an uncommitted edit is nobody's decision. A feature branch whose copy names a different trunk from the base's own — the line edited on one side after the cut — takes its own copy; nothing re-reads the line after the sync.
- **A found line is never skipped.** The agent's `grep` is wider than the format — bulleted, indented and any-case `base branch:` lines — so a malformed line reaches the script instead of being missed. A line not in the exact form, two lines naming different branches, `HEAD`, and a readable line naming a branch absent from `git for-each-ref refs/heads/` each stop at preflight before preflight's agent, with no handoff, naming the line; the reason is the owner's *a stale line is a stop, not a silent fallback*, applied to an unreadable one on the same ground — a skipped line would hand the base to the sources it was written to overrule. The local branch list replaces the per-name `show-ref` loop, so the trunk names and the line's branch are checked against one report.
- **Counts.** One reason and one exit added — the unreadable or stale line: twenty reasons across thirty-one exits, eleven of them preflight's.
- **Template.** `java-backend-template` `00639a9` puts `` Base branch: `main` `` in both `CLAUDE.md` files beside the definition-of-done command — `main` because the scaffold runs `git init -b main`, CI triggers on `main` and the ruleset protects it — and `new-java-backend` pins it. A vendored scaffold from that commit, `--skip-verify`, `TEMPLATE_URL` set to the local clone, carried the line at the project root, and the discovery commands run there resolved `main` from it.

Stub: 313 checks pass on the edited script, every from/until pair `done` with the base taken from the line; 47 are new, of which 37 fail on `1677ab8`, and the other ten are three controls for behaviour meant not to change, three stale-line cases where `1677ab8` also stops for another reason, three readings of this skill, the template and the pin, and one fact about git. Six earlier checks changed with the behaviour: the unresolved stop's fix, now the line and not `git remote set-head`, on its five fixtures, and the discovery prompt's branch command. The new checks include the prompt's own `grep` run over a sample file and the three commands run in scratch repositories — a feature branch cut before and after the line, a start on `dev`, an uncommitted line, a stale one — with their output fed to the script.

**2026-09-25, adversarial review of the base-branch discovery and the `CLAUDE.md` line.** Ground: `1677ab8`, `d5bafa6` and `java-backend-template` `00639a9`, read whole; the discovery agent's three commands and preflight's git steps run by hand, in the prompts' words, in scratch clones of the three service repositories (a bare fake origin holding each repository's `origin/*` refs, the local heads copied, `origin/HEAD` unset); the originals read only through `git show`, `git log` and `git for-each-ref`. **No Workflow run.**

- **What held.** On `dev` in each clone the three commands print `` Base branch: `dev` ``, nothing, and `refs/heads/dev` `refs/heads/main`, and the script resolves `dev` from the line. A new feature from `dev` (`reference-data` `specs/005-address-registry`) is made as `feature/005-address-registry`; a later start on `dev` whose feature branch is ahead and holds `plan.md` and `tasks.md` checks it out, and with a spec edited on `dev` since, merges it and reports the spec changed; a close-out of a feature merged into `dev` (`reference-data` 004, `product-catalog` 004, `customer-party-adapter` 003, with `featureDir`) makes the feature branch at `dev`'s HEAD. `dev` did not move in any of them. The prompt's `grep` prints exactly the line, or nothing, on 39 real `CLAUDE.md` files — every branch of the three repositories and their `backend/` copies, the template's two at `00639a9`, this repository's own — so no prose mention stops a run today. The Claude Code `rtk` hook on the owner's machine leaves the piped `git show … | grep` output intact. Both template placements resolve `main` from a `--skip-verify` scaffold of each mode against the local template clone.
- **Defects found and fixed.** The unresolved stop told a branch cut before the line to *start the run from the base branch*; preflight resolves the feature directory in the tree it starts in, so that fails wherever the spec is on the feature branch only — which is the one live pre-line branch, `customer-party-adapter`'s remote-only `feature/004-employee-registry` (two commits ahead of `dev`, the spec among them; `reference-data`'s four `feature/spec-0N-…` branches are merged into `dev`). The remedy is now a merge of the base, or `baseBranch` once, whose sync carries the line into the branch; confirmed in a clone, where after that merge the branch resolves `dev` with no argument. The `grep` skipped the emphasised (`**Base branch:**`) and numbered forms, so such a line fell through to `origin/HEAD` although the stop text said a line of any other form is a stop; it now prints them, and headings, quotes and table rows stay unseen. `origin/HEAD` was taken as the base without a local branch of that name: `git clone -b dev` of a remote whose default is `main` reports `origin/main` and only a local `dev`, and preflight, which merges the local base and fetches nothing, would read `dev` as a feature branch with no base to merge — the defect `1677ab8` removed, through another door; it is now the unresolved stop, naming `git branch <name> origin/<name>`, and does not fall through to the trunk names, which would answer `dev`. A later start on the base branch with no `featureDir` resolves only a specified-but-unplanned directory, so `converge-feature` on `dev` in `reference-data` resolved `005-address-registry` and stopped on its missing `plan.md` with a restart of `from: "plan"` that would build it; the stop now says how the feature was resolved and names `featureDir`, and both skills say a close-out on the base branch passes it. The `SKILL.md` sentence saying the three repositories hold no line was stale by the time it shipped: the line is on the local `dev` of each (`205fdd6`, `02ae035`, `931f9c3`) and on no `origin/dev`.
- **Not changed.** Two identical lines resolve rather than stop; a pre-line branch still stops rather than reading the line from the trunk branches; the service repositories' own `CLAUDE.md` still says feature work lives *ahead of `main`* and that CI rulesets protect `main`, beside a line naming `dev`. Each is a question for the owner, in the review's report.

Stub: `h10`, 343 checks pass on the edited script; 30 are new, of which 12 fail on `d5bafa6` — the other eighteen are controls, facts about the real repositories, and preflight's git steps run by hand in scratch clones, and four earlier checks changed with the behaviour — the unresolved stop's remedy on three fixtures and the discovery prompt's `grep`; one fixture changed to model a clone whose `origin/HEAD` names a local branch.
