# Every text file is LF in the working tree on every machine, whatever that
# machine's core.autocrlf says. This repo copies text out of raw/ into skills
# byte-identically and gates the copy with a diff; a gate whose answer depends on
# which computer ran it is not a gate.
* text=auto eol=lf
