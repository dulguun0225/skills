# Every text file is LF in the working tree on every machine, whatever that
# machine's core.autocrlf says. The skills here are diffed and grepped as the
# only checks over them; a check whose answer depends on which computer ran it
# is not a check.
* text=auto eol=lf
